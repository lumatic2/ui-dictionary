# PLAN — M28: CLI 0.4.1 폴리싱 — 킥스타트 handoff 거짓말 제거 + verify 오탐 제거

> 생성: 2026-08-05 · 갈래: 신규 goal `cli-polish-041` (1/1) · scope: 0.4.0 출고 후 실사용에서 드러난 CLI 결함 4건을 한 patch 로 닫는다 — 킥스타트가 인쇄하는 handoff 가 실제 프로젝트와 일치하고, verify 가 토큰 준수 코드를 위반으로 부르지 않게 한다.
Status: approved (사용자 승인 2026-08-05 "ㄱㄱ" — 결정 A = (a) 관측 후 지목분만 확정)

## 북극성 → milestone → step (위계)

- **북극성**: Askewly Design (← `CLAUDE.md` 「북극성」 절) — "일회성 작업에서 → 반복 가능한 루프로" 축. 킥스타트는 이 시스템이 남에게 이식되는 **유일한 원커맨드 표면**이다. 그 표면이 인쇄하는 안내가 틀리면 첫 3분에서 신뢰를 잃는다 — M27 E2E 에서 실제로 신선 빌드가 1회 실패했다.
- **왜 지금 묶는가**: 4건 전부 **0.4.0 배포본에서만 관측되는 결함**이고, CLI 는 문서와 달리 재배포해야 반영된다(`cli-release-procedure.md` §"왜 재배포가 자주 필요한가"). 개별로 내면 patch 릴리스가 4회 — 한 번에 닫고 0.4.1 한 번 낸다.
- **입력 실측 (2026-08-05 계획 탐색 — 전건 재현 확인)**:
  - ① **verify 오탐** — `examples/ui-vocabulary-site/src/components/ui/chart.tsx:70` 의 Tailwind arbitrary variant `[&_.recharts-cartesian-grid_line[stroke='#ccc']]:stroke-border/50`. `#ccc`·`#fff` 는 **recharts 내장 기본색을 겨냥한 CSS 속성 셀렉터**이지 색 선언이 아니다. 같은 줄이 그것을 `stroke-border` 로 덮는 코드다 — 즉 verify 가 **토큰 준수를 위반으로 부른다.** 해당 파일 68행에 이미 사람이 남긴 해명 주석이 있다(`hex 는 색 사용이 아니라 recharts 내장 기본색(#ccc·#fff)을 겨냥해…`). 규칙: `src/verify.ts` L35 `HEX_RULE`, 마스킹 지점 `maskIgnoredRegions` L64.
  - ② **export 이름 하드코딩** — `src/kickstart.ts:459` 가 블록과 무관하게 `import { SaasAppShell }` 을 인쇄한다. `marketing-landing` 의 실제 export 는 `MarketingLandingPage`(+`MarketingLandingDemo`) — **존재하지 않는 심볼을 안내한다.** 두 블록 모두 `page.tsx` 에 `export function <Name>` / `export function <Name>Demo` 형태(2026-08-05 `/r/*.json` 실측)라 파생 규칙이 성립한다.
  - ③ **npm dep 누락** — `src/kickstart.ts:363` 은 registry 항목의 **선언된** `dependencies` 만 모은다. 그런데 shadcn 원본 `button.json`·`badge.json`·`accordion.json` 의 선언은 `["radix-ui"]` 뿐인데 그 `button.tsx` 본문은 `import { cva } from "class-variance-authority"` 를 쓴다(2026-08-05 라이브 fetch 실측). 인쇄된 `npm i` 를 그대로 따르면 **cva 미설치로 신선 빌드가 깨진다** — M27 에서 실제로 1회 발생. 우리 registry 자산 중 cva 사용은 0건이므로 **원인은 전부 shadcn 상류 선언 누락**이고, 재선언이 아니라 **이식된 파일의 import 실측**으로 메워야 한다.
  - ④ **킥스타트 기본값 시각 품질** — M19 사용자 관측 코멘트 "나쁘진 않은데 더 다듬으면 좋겠다"(게이트는 통과). finding 큐 등재 문구: 팔레트 미세조정·섹션 여백·카드 그라디언트 강도(`evidence/reusable-composition/m19-kickstart.md` L25). **취향 = 사용자 소유** — 결정 A 로 승인 시 확정한다.
- **조사 인용 (재리서치 불요 — M12 규약)**: 신규 외부 조사 없음. 이번 건은 전부 자체 코드의 재현된 결함이고 판정 기준은 기존 계약 문서(`cli-release-procedure.md`·`block-contract.md` §8)가 이미 소유한다. → **조사 불요 — 재현 경로가 이미 확보된 자체 결함 수정.**

## run 전 scope 결정

- **포함**: ① verify 속성 셀렉터 오탐 제거 ② 킥스타트 handoff 2건(export 이름 파생 · 이식 파일 import 실측 dep 집계) ③ 킥스타트 기본값 폴리싱(결정 A 범위) ④ `0.4.1` 출고 + 라이브 재현.
- **제외**: 새 verify 규칙 추가 · 킥스타트 신규 플래그·대화 흐름 개편 · 세 번째 블록(docs-site) · dark/light SSOT 파생 · recharts `isAnimationActive` 노출 검토(승계 finding) · Figma 후속 3건 · 블록 소스의 **구조** 변경(폴리싱은 토큰·여백값 범위에 한정 — 섹션 추가·재배열은 별건).
- **연쇄**: 없음 — 이 milestone 단독. 완료 시 큐 잔여(docs-site 블록 · dark/light SSOT · 책 스터디 · D2 · Around)는 `/harness-done` 에서 후보 제시만.
- execution mode: continuous
- **중단점**: completed / 증거가 있는 blocked / decision_required / risk_gate / user_stopped. **human gate 2개** — step-3 폴리싱 관측, step-4 npm publish 직전 승인.
- rollback/cleanup: 커밋 단위 revert. npm publish 는 **되돌릴 수 없다**(`cli-release-procedure.md` — unpublish 후에도 `name@version` 영구 재사용 불가) → 사전 게이트 전건 통과 + 사용자 승인 후에만. E2E 신선 프로젝트는 scratchpad(레포 무오염). **세션 종료 시 dev 서버·정적 서빙 프로세스 정리 의무**(2026-08-04 실사고 — 잔존 서버가 registry 재생성을 EPERM 차단).

## 스캐폴딩 결정

- source-of-truth: 검사 규칙 SSOT = `packages/cli/src/verify.ts` · 킥스타트 SSOT = `packages/cli/src/kickstart.ts` · 출고 절차 정본 = `docs/design-system/cli-release-procedure.md` · 킥스타트 계약 정본 = `docs/design-system/block-contract.md` §8. 폴리싱이 블록 소스에 닿으면 그 SSOT 는 `examples/ui-vocabulary-site/src/components/blocks/<block>/` 이고 배포 표현은 `/r/<block>.json`(재생성 필요).
- 검증: step 별 Verify + **통합 E2E** = 라이브 `npx --yes @askewly/design@0.4.1 init <빈 dir> --block marketing-landing --color teal --yes` → **인쇄된 `npm i` 를 그대로 실행** → `npm run build` 성공 → `verify` 0건 → 인쇄된 export 심볼이 이식 파일에 실존. 회귀 = `npx vitest run` 전건(현행 60) + `npx tsc --noEmit` exit 0 + `verify` 를 사이트 전체(`examples/ui-vocabulary-site/src`)에 돌려 **오탐 감소분만** 발생(신규 통과·신규 위반 0).
- 배포/운영: npm publish 1회(`0.4.1`, patch — 오탐·누락 수정이므로 semver 표 patch 행). git push 는 세션 일괄(deploy-batching 규약, 사전 요약 보고 후). 블록 소스 폴리싱이 발생하면 registry·llms 재생성 커밋 + CF Pages 배포.
- 자기선언 도메인 — 오탐 처리 방식: 예외는 **마스킹 층**이 소유한다(`maskIgnoredRegions`). 파일 SKIP 목록·인라인 마커를 늘리지 않는다 — 기존 설계 주석이 이유를 이미 적어 뒀다("Attaching per-rule exceptions instead would mean re-implementing the same context handling for every future rule"). 기술 결정 ①.
- 자기선언 도메인 — dep 집계 권위: registry 선언(`dependencies`)은 **하한**이고, 실제 설치 목록의 정본은 **이식된 파일의 import 실측**이다. 상류(shadcn) 선언을 신뢰하지 않는다. 기술 결정 ②.
- 검토 후 제외: registry 생성기(`scripts/generate-registry.mjs`) 수정 — 우리 자산의 dep 선언은 정확하고(cva 사용 0건 실측) 결함은 상류에 있다. verify 임계·규칙 세트 개정 — 이번은 오탐 1종만.

## 결정 로그

- status: resolved
- **[승인 확정 2026-08-05]** 사용자 승인("ㄱㄱ", 대안 미지정): 결정 A = **(a) 관측 후 지목분만**. step-3 은 기본값 산출물을 실브라우저로 띄워 사용자가 지목한 항목만 수정하고, 지목이 블록 소스에 닿으면 registry·llms 재생성까지 그 step 안에서 처리한다.
- **결정 A — ④ 킥스타트 기본값 폴리싱의 범위 [사용자 소유 · 취향]**: M19 코멘트가 "더 다듬으면 좋겠다"까지만이라 무엇을 다듬을지가 비어 있다. 팔레트는 CLI(`kickstart.ts` ACCENTS/CANVASES)가 소유하지만 **여백·그라디언트 강도는 블록 소스**가 소유해서, 선택에 따라 registry 재생성·재배포가 딸려온다.
  - **(a) 관측 후 지목분만 [추천]** — step-3 에서 기본값 산출물을 실브라우저로 띄우고, 사용자가 눈으로 보고 지목한 것만 고친다. 범위가 실물에 묶여 커지지 않고, 취향 결정을 계획 단계 추정으로 채우지 않는다. 지목이 블록 소스에 닿으면 registry 재생성까지 이 step 안에서 처리.
  - (b) 에이전트 후보 제시 후 선택 — finding 큐 3항(팔레트·여백·그라디언트) 각각에 before/after 후보를 만들어 고르게 한다. 왕복이 늘고, 사용자가 실제로 거슬려한 지점이 후보 밖일 수 있다.
  - (c) 이번 묶음에서 제외 — 버그 3건만 닫고 0.4.1 을 빨리 낸다. 폴리싱은 별도 milestone. 취향 왕복이 릴리스를 늦추지 않는다는 게 장점.
- **기술 결정 (사전 소진 — 재질문 없음)**:
  - ① **오탐 처리 = 마스킹 확장**. `maskIgnoredRegions` pass 3 으로 **속성 셀렉터 안의 hex 만** 공백화한다: `[<attr><연산자>'#hex']` / `"#hex"` 형태(예: `[stroke='#ccc']`). 셀렉터 밖의 `#ccc` 는 그대로 위반. 큐 문구는 "예외/SKIP 목록"이었으나 **파일 단위 SKIP 은 채택하지 않는다** — 한 파일을 통째로 면제하면 그 파일의 진짜 위반까지 같이 숨는다(chart.tsx 는 실제로 다른 색 선언을 가질 수 있다).
  - ② **dep 집계 = 이식 파일 import 실측 ∪ 선언 dep**. `transplantBlock` 이 쓰는 각 파일 내용에서 bare specifier(`import … from "pkg"` / `"@scope/pkg"` — 상대경로·`@/` alias 제외)를 추출해 `npmDeps` 에 합류. 하위 경로는 패키지 루트로 접기(`radix-ui/react-slot` → `radix-ui`). cva 하드코딩은 하지 않는다 — 같은 결함이 다음 primitive 에서 또 난다.
  - ③ **export 이름 파생 = 블록 `page.tsx` 실측**. 이식된 `components/blocks/<block>/page.tsx` 에서 `export function <Name>` 중 `Demo` 로 끝나지 않는 첫 이름을 쓴다. 못 찾으면 심볼을 지어내지 말고 파일 경로만 안내한다(`@/components/blocks/<block>/page` 에서 export 확인). 대안(registry `meta.exportName` 추가)은 기각 — 자산 56종 재생성·재배포가 딸려오고, CLI-only 로 닫을 수 있는 문제다.
  - ④ **버전 = `0.4.1` (patch)** — 오탐 수정·누락 수정·메시지 개선은 `cli-release-procedure.md` semver 표의 patch 행. CLI 인터페이스(플래그·출력 형식·exit code 의미) 변경 없음.
  - ⑤ **E2E registry 는 라이브** — 이번 변경은 CLI 전용이라 registry 재배포 선행이 불필요하다. 단 결정 A 가 (a) 이고 지목이 블록 소스에 닿으면 그때는 로컬 정적 서빙(`python -m http.server 8899 --directory examples/ui-vocabulary-site/public` → `--registry http://127.0.0.1:8899`, M27 기술결정 ④ 승계)으로 먼저 확인하고 push·배포 후 라이브 재확인.

## Step 트리

- [x] **step-1 — verify 속성 셀렉터 오탐 제거**
  - Artifact: `packages/cli/src/verify.ts` 의 `maskIgnoredRegions` 에 속성 셀렉터 마스킹 pass 추가(기술 결정 ①) + 설계 주석 1문단(왜 셀렉터 안의 hex 는 색 사용이 아닌가) + `packages/cli/test/verify.test.ts` 케이스 추가: ⓐ `[&_.recharts-cartesian-grid_line[stroke='#ccc']]:stroke-border/50` → 위반 0 ⓑ 같은 파일의 `color: #ccc` → 여전히 위반 1 ⓒ 큰따옴표 형태 `[stroke="#fff"]` → 위반 0 ⓓ 셀렉터 밖 `#ccc` 단독 → 위반 1.
  - Files: edit packages/cli/src/verify.ts. edit packages/cli/test/verify.test.ts.
  - Risk: 기계적 (검사기 국소 수정 — 과다 마스킹이 유일한 위험이고 ⓑⓓ 케이스가 그것을 잡는다)
  - Dependencies: 없음
  - Verify: `cd packages/cli && npx vitest run` 전건 PASS(신규 4케이스 포함) + `npx tsc --noEmit` exit 0 + 빌드본으로 `examples/ui-vocabulary-site/src` 전체 스캔 시 **chart.tsx 의 `[stroke=…]` 위반만 사라지고 다른 파일 판정 불변**(전/후 위반 목록 diff 로 확인).
  - Failure probe: 마스킹이 너무 넓어 `[&_.foo]:bg-[#ccc]`(Tailwind arbitrary **값** — 이건 진짜 위반)까지 삼키는 경우. 이 케이스를 테스트에 넣어 **위반 1 로 남는지** 확인한다. 삼키면 마스킹 패턴을 `속성명=따옴표hex` 로 더 좁힌다.
  - Commit: changeset `20260805-m28-cli-polish-041` (README 절: step-1).

- [x] **step-2 — 킥스타트 handoff 정확도 (export 이름 파생 + import 실측 dep 집계)**
  - Artifact: `packages/cli/src/kickstart.ts` — ⓐ `transplantBlock` 이 쓰는 파일 내용에서 bare npm specifier 를 추출해 `npmDeps` 에 합류(기술 결정 ②, 상대경로·`@/` alias 제외, 하위경로는 루트로 접기) ⓑ 이식된 `blocks/<block>/page.tsx` 에서 export 심볼 파생(기술 결정 ③, 미발견 시 심볼 없는 안내로 폴백) ⓒ `Next steps` 3행이 그 파생값을 쓰도록 교체. + 단위 테스트: 추출 함수(하위경로 접기·alias 제외·scoped 패키지)와 export 파생(정상·`Demo` 만 있는 경우·미발견 폴백).
  - Files: edit packages/cli/src/kickstart.ts. edit(또는 write) packages/cli/test/kickstart.test.ts.
  - Risk: 기계적 (출력 문자열 + 순수 함수 2개 추가. 이식 로직 자체는 불변)
  - Dependencies: 없음 (step-1 과 독립 — 동시 가능)
  - Verify: `npx vitest run` 전건 PASS + `npx tsc --noEmit` exit 0 + 로컬 빌드본으로 신선 scratchpad 디렉터리에 `init --block marketing-landing --yes` 실행 → 인쇄된 `npm i` 목록에 **`class-variance-authority` 포함** + 인쇄된 export 이름이 `MarketingLandingPage`(이식 파일 grep 으로 실존 확인) + 같은 절차를 `--block saas-app-shell` 로 반복해 `SaasAppShell` 이 여전히 맞게 나오는지(회귀).
  - Failure probe: `import type { X } from "pkg"` / `export … from "pkg"` / 동적 `import("pkg")` 를 놓쳐 dep 이 또 빠지는 경우 — 세 형태를 테스트에 넣는다. 반대로 Node 내장 모듈(`node:fs` 등)을 npm 패키지로 잘못 집계하는 경우도 테스트로 막는다.
  - Commit: changeset (README 절: step-2).

- [x] **step-3 — 킥스타트 기본값 폴리싱 (결정 A 범위) + 사용자 관측**
  - Artifact: 로컬 빌드본으로 신선 프로젝트를 킥스타트해 dev 서버로 띄우고 **사용자 기본 브라우저로 열어 관측**(라이트/다크). 결정 A 선택에 따라: (a) 사용자가 지목한 항목만 수정 — 팔레트면 `kickstart.ts` 의 `ACCENTS`/`CANVASES`, 여백·그라디언트면 블록 소스 + registry·llms 재생성 / (c) 이면 이 step 은 **관측만 하고 수정 없음**(기준선 스크린샷만 evidence 에 남김). 산출: `evidence/cli-polish-041/m28-kickstart-defaults.md`(전/후 스크린샷 경로·지목 항목·판정).
  - Files: write evidence/cli-polish-041/m28-kickstart-defaults.md. (지목 시) edit packages/cli/src/kickstart.ts 또는 examples/ui-vocabulary-site/src/components/blocks/<block>/*.tsx + registry 재생성 산출물.
  - Risk: 위험 (취향 결정 — 사용자 관측이 유일한 판정자이며, 확인 전 완료 선언 금지)
  - Dependencies: step-2 (킥스타트 출력이 고쳐진 상태에서 관측해야 같은 화면을 두 번 안 본다)
  - Verify: 실브라우저 관측 1회 + 라이트/다크 스크린샷 ≥2장 + 수정이 있었으면 `verify` 0건 유지 + 블록 소스에 닿았으면 `node scripts/generate-registry.mjs` 기존 자산 diff 0(변경 블록 제외) + `node scripts/check-llms-sync.mjs` PASS + 사이트 `npm run build` PASS + **사용자 관측 통과**.
  - Failure probe: 팔레트를 다듬다 WCAG AA(4.5:1)가 깨지는 경우 — 변경한 accent 는 라이트/다크 각각 `--primary`/`--primary-foreground` 대비를 계산해 4.5:1 미만이면 되돌린다. 취향이 접근성을 이기지 않는다.
  - Commit: changeset (README 절: step-3).

- [ ] **step-4 — `0.4.1` 출고 + 라이브 재현**
  - Artifact: `packages/cli/package.json` 버전 `0.4.1` + `cli-release-procedure.md` 배포 이력 행 + 사전 게이트 전건 실행 로그 → **사용자 승인 후** `npm publish` → 라이브 재현 E2E → `evidence/cli-polish-041/m28-release-041.md`.
  - Files: edit packages/cli/package.json. edit docs/design-system/cli-release-procedure.md. write evidence/cli-polish-041/m28-release-041.md.
  - Risk: 위험 (npm publish 는 되돌릴 수 없다 — `name@version` 영구 점유. 사전 게이트 전건 PASS + 사용자 명시 승인 없이 실행 금지)
  - Dependencies: step-1, step-2, step-3
  - Verify: 배포 전 — `npm run build` · `npx vitest run` 전건 · `npx tsc --noEmit` exit 0 · `npm pack --dry-run` 담기는 파일 목록이 0.4.0 대비 예상 밖 증감 없음. 배포 후(**통합 E2E**) — 빈 디렉터리에서 `npx --yes @askewly/design@0.4.1 init <dir> --block marketing-landing --color teal --yes` → 인쇄된 `npm i` 를 **그대로 실행** → 인쇄된 export 로 렌더하는 최소 진입점 작성 → `npm run build` 성공(cva 누락 무재현) → `npx @askewly/design@0.4.1 verify <dir>` 0건.
  - Failure probe: publish 는 됐는데 라이브 E2E 가 실패하는 경우 — 되돌리기가 없으므로 `0.4.2` 정정 릴리스로 처리하고(절차 문서 명시 방침) 그 사실을 evidence 에 남긴다. 사전 게이트를 통과했는데 라이브에서 깨졌다면 게이트에 빠진 검사가 무엇인지도 함께 기록.
  - Commit: changeset (README 절: step-4).

## 검증/DoD

- **DoD**: 0.4.1 이 npm 에 올라가 있고, **빈 디렉터리 → 킥스타트 1커맨드 → 인쇄된 `npm i` 그대로 → 빌드 성공 → 인쇄된 export 로 렌더** 가 개입 없이 통과한다(M27 에서 깨졌던 바로 그 경로). 동시에 verify 가 chart.tsx 계열 속성 셀렉터를 위반으로 부르지 않으면서 진짜 색 선언은 계속 잡는다. 결정 A 가 (a)/(b) 면 폴리싱 사용자 관측 1회 포함.
- **실패 모드 확인**: ① 마스킹 과다 — `bg-[#ccc]` 는 여전히 위반(step-1 probe) ② dep 집계 과소·과다 — `import type`·동적 import 포함, `node:` 내장 제외(step-2 probe) ③ export 미발견 시 심볼 날조 없이 폴백(step-2 테스트) ④ 폴리싱이 WCAG AA 를 깨지 않음(step-3 probe).
- **Evidence**: `evidence/cli-polish-041/m28-kickstart-defaults.md` · `evidence/cli-polish-041/m28-release-041.md`
- **회귀 게이트**: vitest 전건(현행 60 + 신규) · `tsc --noEmit` exit 0 · `saas-app-shell` 킥스타트 경로 무변경 확인 · 사이트 전체 verify 판정이 오탐 감소분 외 불변 · (블록 소스 수정 시) registry 기존 자산 diff 0 + llms 동기화 PASS.

## finding 큐

- (실행 중 발견분 적재)

## 진행 로그

- 2026-08-05 step-3 완료 — 오너 지목 3건(그라디언트 제거·슬롯 테두리 제거·마키 중앙정렬+hover 정지). 팔레트·캔버스 지목 없음(CLI 무변경). hover 원인 = 쌓인 variant 우→좌 적용. 측정 전/후 4항 + 라이트/다크 관측.
- 2026-08-05 step-2 완료 — import 실측 집계+export 파생. marketing-landing 에 cva·motion·react 추가 등장, export 는 SaasAppShell(부재) → MarketingLandingPage. saas-app-shell 회귀 없음. vitest 71/71·tsc 0.
- 2026-08-05 step-1 완료 — 마스킹 pass 추가(속성 셀렉터 hex). vitest 63/63·tsc 0·사이트 스캔 154→153 (chart.tsx:70 한 줄만 소멸, 나머지 판정 동일). 1차 구현이 `\"` 이스케이프 형태를 놓쳐 fixture 가 적발 → 패턴 보정.
- 2026-08-05 작성 — 4건 전건 재현 확인(chart.tsx L70 셀렉터 · kickstart.ts L459 하드코딩 · shadcn button.json 선언 `["radix-ui"]` vs 본문 cva import · M19 관측 코멘트).
