# PLAN — M26: CLI 0.4.0 출고 — 킥스타트 라이브 소비 마개

> 생성: 2026-08-04 · 산출물: changeset (버전·출고·실증) · scope 결정: `@askewly/design` 0.3.0 → 0.4.0 minor 출고 + 레포 밖 라이브 실증. 사용자 지시 2026-08-04 "1,2번 ㄱㄱ" (M25 와 chain — M25 마감·push 후 착수).
> milestone-레벨 durable plan doc. 절차 정본: `docs/design-system/cli-release-procedure.md` (Trusted Publishing).

Status: approved (사용자 승인 2026-08-04 "ㄱㄱ" — M25+M26 연쇄, M25 마감 후 착수)

## 북극성 → milestone → step (위계 — 2계층)

- **북극성**: Askewly Design — 킥스타트 라이브 소비의 마지막 마개 (← `CLAUDE.md` 「북극성」 절). registry(55 예정)·cosmos 팔레트·블록은 라이브인데 CLI 는 npm 0.3.0(cosmos·--block 킥스타트 미포함)이라 소비 경로가 로컬 한정이다.
- **milestone**: 0.4.0 minor(새 기능: init --block 킥스타트·--color cosmos·데이터 갱신) 출고 + 레포 밖 실증(verify exit 계약 + 킥스타트가 라이브 registry 를 --registry 플래그 없이 소비). 리프 판정: 독립 step 2(게이트+버전 / 출고+실증) + 통합 검증(레포 밖 실증) + 단독 capability(남의 머신에서 원커맨드 킥스타트).
- **조사 인용**: `docs/design-system/cli-release-procedure.md` — Trusted Publishing(`gh workflow run publish-cli.yml`), 버전 정책 표(minor = 새 서브커맨드·데이터 갱신), 배포 후 3단 검증(전파≠성공 실측 2026-07-22).

## run 전 scope 결정 (확정)

- **결정**: 0.4.0 단일 출고. 제외: CLI 기능 추가·verify 오탐 수리(chart.tsx finding — 별도 patch 로), README 대개편.
- **execution mode**: `continuous`
- **중단점**: completed / blocked(워크플로 실패·전파 불가) / user_stopped. human gate = 실증 결과 관측(최종 보고).
- **rollback/cleanup**: npm 은 unpublish 불가(정본 명시) — 잘못 나가면 다음 patch 로 정정. 워크플로 실패 시 원인 수리 후 재실행(버전 재사용 가능 — publish 전 실패 한정).

## 스캐폴딩 결정

- source-of-truth: `packages/cli/package.json` version · 출고 경로 = `.github/workflows/publish-cli.yml`(OIDC).
- 검증: 정본의 배포 전 4커맨드(build·vitest·tsc --noEmit·pack --dry-run) + 배포 후 3단(전파 200·npm view 버전·레포 밖 npx 실증) + 킥스타트 라이브 실증 1회.
- 배포/운영: 버전 커밋 push 후 워크플로 실행(미푸시 커밋은 배포되지 않음 — 정본 명시).
- 위임: skip — 단일 경로 절차.
- 검토 후 제외: 구 토큰 경로(2027-01 폐지 예정, Trusted Publishing 실적 0.3.0 있음).

## 결정 로그 (run 전 사전 소진)

- 결정 1 — 버전 = **0.4.0 (minor)**: 0.3.0 이후 신규 = init --block 킥스타트 경로·ACCENTS cosmos·registry meta.requiredCssVars 소비·번들 데이터 갱신 → 정책 표의 "새 서브커맨드·데이터 갱신" 해당 → **확정**.
- 결정 2 — 실증 시나리오(기술): ① `npx @askewly/design@0.4.0 verify` exit 1/0 계약 ② 레포 밖 임시 디렉터리에서 `npx @askewly/design@0.4.0 init <dir> --block saas-app-shell --color cosmos --yes` — **--registry 플래그 없이** 라이브 `ui.askewly.com/r` 소비 → verify PASS 까지 → **확정**.
- status: resolved

## Step 트리

- [x] **step-1 — 버전 0.4.0 + 배포 전 게이트 + push**
  - Artifact: package.json 0.4.0 + 정본 4커맨드 게이트 통과 + 버전 커밋 push.
  - Risk: 기계적 (로컬 게이트)
  - Files: edit packages/cli/package.json. run npm run build·npx vitest run·npx tsc --noEmit·npm pack --dry-run.
  - Dependencies: none (milestone 전제: M25 마감·push 완료 — chain 이 소유)
  - Verify: vitest 전건 PASS + tsc exit 0 + pack 목록에 dist/·data/ 포함 & src/·test/·scripts/ 부재 + push 완료.
  - Failure probe: pack 목록에 금지 항목 검출 시 중단·files 배열 수리.
  - Commit: changeset `20260804-m26-cli-release-040` (README 절: step-1) — `chore(release): cli 0.4.0`.
- [x] **step-2 — Trusted Publishing 출고 + 레포 밖 실증**
  - Artifact: `gh workflow run publish-cli.yml --ref main` → watch 통과 → 전파·버전 확인 → 레포 밖 임시 디렉터리 실증 2종(verify exit 계약 + 킥스타트 라이브 소비 → 산출물 verify PASS) → 절차 정본에 0.4.0 실적 추기.
  - Risk: 위험 (공개 레지스트리 — 되돌리기 불가. 방어 = step-1 게이트 + 워크플로 내장 게이트)
  - Files: run gh workflow run/watch·curl·npm view·npx(레포 밖 scratchpad). edit docs/design-system/cli-release-procedure.md(실적 1줄).
  - Dependencies: step-1
  - Verify: `npm view @askewly/design version` = 0.4.0 + registry URL 200 + 레포 밖 verify exit 1(위반 dir)/exit 0(clean) + 킥스타트 라이브 실구동 산출물(DESIGN.md·블록 이식·verify PASS) 기록.
  - Failure probe: 전파 지연(최초 배포 실측 ~3분) — 404 면 최대 10분 재시도 후에도 404 시 blocked 기록. 킥스타트 라이브 실패 시 원인(캐시·경로) 분리 기록.
  - Commit: changeset (README 절: step-2).

## 재생성 장벽

- (없음 — 사이트 재생성 없음. CLI build 는 step-1 내부.)

## 검증/DoD

- **DoD**: npm 0.4.0 라이브 + 레포 밖 실증 2종 통과(verify exit 계약·킥스타트 라이브 소비) + 절차 정본 실적 추기. 실패 모드 검증 = pack 금지 항목 probe + 전파 404 경로 명시.

## 수치 출처

- 0.3.0(현행) = `node -p "require('./packages/cli/package.json').version"` + 정본 실적표(2026-08-01).
- 배포 전 4커맨드·후 3단 = cli-release-procedure.md 정본 절차.

## finding 큐 (작업 중 발견 — 다음 step/changeset 으로 흘림)

- (승계) CLI verify chart.tsx 속성 셀렉터 오탐 — 0.4.x patch 후보.

## 진행 로그 (append-only)

- 2026-08-04 · 계획 작성 — 소스 0.3.0 실측, Trusted Publishing 실적(0.3.0, 1분 28초) 확인.
- 2026-08-04 · step-1 완료 — 0.4.0, build/vitest 60/60/tsc/pack(15f·258kB) 전건 PASS.
- 2026-08-04 · step-2 완료 — 워크플로 success·npm 0.4.0·전파 200·레포 밖 실증 2종(verify exit 1/0·킥스타트 라이브 34파일 이식 verify PASS). M26 DoD 충족 → /harness-done.
