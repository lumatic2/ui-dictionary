# Plan - CLI2 프로젝트 주입 (init / add)

Date: 2026-07-10
Milestone: CLI2 (`ROADMAP.md`, active — horizon-run 연쇄 승격)

## 위계

- Objective: `docs/OBJECTIVE.md` — 장기 아크 5 "에이전트 통합"
- Horizon: `docs/horizons/2026-07-agent-design-cli.md` (active)
- Milestone: CLI2 — 프로젝트 주입 (init / add)
- 입력 계약: `docs/design-system/cli-registry-contract.md` §3

## Scope

`init`(대상 프로젝트에 DESIGN.md·tokens.css 생성)과 `add <recipe>`(레시피 Code 섹션 발췌 주입) 구현 + 외부 스크래치 프로젝트 실증. verify 커맨드·npm publish는 CLI3.

**설계 조정 (실사 반영)**: 계약 문서 §3은 `add`가 code_asset 파일을 복사한다고 했으나, 실사 결과 5종 중 2종(showcase-card·landing-hero)의 code_asset이 거대 페이지 파일(home-page.tsx)이라 전체 복사가 비현실적. **균일 정책으로 변경: `add`는 레시피 `## Code` 섹션의 fenced 발췌(recipe-format상 "구조 계약, 복붙 가능 설계")를 `<out>/<id>.tsx`로 주입**하고, code_asset은 "전체 구현 참조" 노트로 출력한다. 계약 문서 §3도 이에 맞게 갱신한다.

중단점: ① 레시피 body 구조가 가정과 어긋나면 blocked ② push는 세션 일괄.

## Step 트리

- [x] Step 1 — 번들 확장: DESIGN.md + askewly.css(@theme inline 바인딩 — 실사 추가 산출물) 번들. (verify: build:data PASS)
- [x] Step 2 — `init`: 3파일 생성 + 충돌 exit 1 + --force. (verify: 임시 디렉터리 + 실패 모드)
- [x] Step 3 — `add`: Code 발췌 주입 + tokens 확인 + Checks/Anti-patterns 노트. 실패 모드 3종 exit 1. (verify: 실행 관측)
- [x] Step 4 — vitest 18/18 + pack fresh-env E2E + 외부 Vite 프로젝트 실증(토큰 경유 violet 렌더, hex 0, 스크린샷). (verify: `changesets/20260710-cli-inject/README.md` 4/4)

## 결정 로그

- [확정, ADR 0004 계승] 범위·이름·위치. 새 사용자 결정 없음.
- [AI 기본값] add 주입물 = Code 섹션 발췌 (위 설계 조정 — 근거: code_asset 2/5가 비주입성 거대 파일).
- 그 외 예상 결정: 없음.
