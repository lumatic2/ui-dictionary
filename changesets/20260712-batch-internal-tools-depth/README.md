
# Batch internal-tools-depth — SD Step 2 (internal-tools surface depth batch)

Target: milestone SD (`docs/plans/2026-07-12-sd-surface-depth.md`, Step 2), procedure `docs/research/reference-loop.md`.

## Scope

Second of four SD surface-depth RL batches, on the `internal-tools` surface. 10 candidates were collected upstream (`docs/research/loop/inbox.yml` staging), with fixed orchestrator adjudications for the 3 recipe promotions, term promotion guidance for 2 more candidates, a default-to-enrichment instruction for 1 candidate, and no-new-artifact handling for the remaining 3. This changeset covers stages 1 (staged into canonical `docs/research/loop/inbox.yml`), 2 (dedup audit run against the staged batch, plus manual neighboring-`terms.yml` reads for every candidate per authoring-workflow.md §3), 3 (adapt: 3 recipes + 2 new terms + 3 no-new-artifact extensions + 1 related-only enrichment), 4 (full verification chain including the FW-added `packages/component-registry` `build:catalog` step), and 5 (ledger row + inbox cleared).

## Contract

- 3 candidates promoted as **recipes**, all internal-tools-surface, semantic tokens only (no hex, no primitive refs):
  - `permission-matrix-editor-grid` (`recipes/data-display/permission-matrix-editor-grid.md`) — sticky dual-axis role×permission editing grid with a diff-before-commit banner and dangerous-permission highlighting. `term_refs: [permission-matrix-editor]` — the existing term already defines the role/permission matrix concept; this recipe is the grid-interaction structure (sticky headers, tri-state cells, diff banner) built on top of it, distinguished by the `-grid` id suffix per fixed adjudication. `code_asset`: `examples/ui-vocabulary-site/src/components/permission-matrix-editor-grid.tsx`.
  - `data-import-wizard-validation-preview-steps` (`recipes/forms/data-import-wizard-validation-preview-steps.md`) — the five-step upload/map/validate/preview/confirm wizard with per-row inline error fix-or-skip. `term_refs: [data-import-wizard-validation-preview, wizard]` per fixed adjudication; distinguished from both by the `-steps` id suffix (the term already existed from a prior RL batch where it was demoted recipe→term for being "too heavy" — this batch completes the recipe). `code_asset`: `examples/ui-vocabulary-site/src/components/data-import-wizard-validation-preview-steps.tsx`.
  - `audit-log-filterable-export-feed` (`recipes/data-display/audit-log-filterable-export-feed.md`) — filterable (actor/action/date-range), filter-scoped-export audit feed. `term_refs: [audit-log, activity-feed]` per fixed adjudication; the Intent and Anti-patterns sections explicitly distinguish it from `activity-feed`'s social/collaboration framing, matching `audit-log`'s own existing `anti_use` note. `code_asset`: `examples/ui-vocabulary-site/src/components/audit-log-filterable-export-feed.tsx`.
- 2 candidates promoted as **new terms** (`docs/ui-vocabulary/terms.yml`):
  - `background-job-queue-monitor` (data-display, `data-timeline-history` group) — symmetric `related` (`compare`) ↔ `retry-panel`, distinguishing the multi-job list-form status monitor from that term's single-failure recovery panel.
  - `webhook-delivery-log-with-retry` (data-display, `data-people-integrations` group) — symmetric `related` (`compare`) ↔ `webhook-endpoint-row`, distinguishing the per-endpoint delivery-attempt history log from that term's single configuration row.
- 1 candidate resolved as **related-only enrichment**, per fixed orchestrator default (no new term unless the neighbor read showed a genuine gap):
  - `ops-command-palette-scoped-actions` — the manual read of `command-palette` found the same overlay/input/grouped-results shape; only the action-list scope (entity-scoped) and the destructive-action visual separation differ, which is a usage detail rather than a new UI structure. Extended `command-palette`'s `description`/`visual_anatomy`/`when_to_use`/`prompt_phrases` to cover the ops variant instead of adding a new artifact. Judgment recorded in the ledger row.
- 3 candidates resolved as **no new artifact** (counted as `duplicates_filtered`), all per fixed orchestrator adjudication:
  - `user-impersonation-switch-flow` → folded into existing `impersonation-session-banner`: extended `description`/`visual_anatomy`/`prompt_phrases` with the entry-confirmation-then-exit flow. No separate related term exists to link to — the flow is internal to the one term, so this was a description/anatomy extension rather than a `related:` array addition.
  - `config-environment-diff-view-recipe-candidate` → no-op. The candidate's observed structure (two-column diff, changed-only filter, added/removed/changed row coding) is already fully covered by the existing `config-environment-diff-view` term's `visual_anatomy` and its pre-existing EN alias "Config diff view" — no term or recipe edit was made.
  - `saved-view-tabs-with-filter-state` → no-op. The tab-as-saved-filter connection this candidate describes is already stated in `advanced-filter-builder-condition-groups.md`'s own Anatomy section ("Save as view: optional action connecting the built filter to `saved-view-tabs`") — no additional `terms.yml` edit was needed since the connection was already documented on the recipe side.
- `docs/research/loop/inbox.yml` restored to the empty template after promotion; `docs/research/loop/ledger.md` gained the `20260712-internal-tools-depth` batch row.

## Verification checklist

- [x] `node scripts/audit-recipe-candidates.mjs` against the staged `docs/research/loop/inbox.yml` → exit 1, 5 errors + 13 warnings. The 5 errors were expected self-matches (the 3 recipe files and 2 new term entries were authored from the fixed orchestrator adjudications, then staged and audited, rather than staged-then-authored) — same phenomenon as `20260712-commerce-depth`'s Amendment 3, recorded here as Amendment 1. The 13 warnings were expected non-fatal near-match/substring/token-similarity risks matching each candidate's own `dedup_hints`, not a regression.
- [x] `python scripts/validate-recipes.py` → exit 0, `recipes ok: 29`.
- [x] `python scripts/validate-ui-vocabulary.py` → exit 0, `terms ok: 558`, `groups ok: 57 (57 in use)`.
- [x] `node scripts/generate-llms-txt.mjs` → exit 0; all 3 new recipes appear under the generated `## Recipes` section (auto-discovered via `recipes/**/*.md` glob), confirmed present in `examples/ui-vocabulary-site/public/llms.txt`.
- [x] `cd examples/ui-vocabulary-site && npm run build` → exit 0, `terms.generated.ts` regenerated (558 terms), `tsc -b && vite build` clean, no new TypeScript errors from the 3 new components.
- [x] `cd examples/ui-vocabulary-site && npm run lint` → exit 0, only the pre-existing shadcn `react-refresh/only-export-components` warnings (6, unchanged set/count from before this batch).
- [x] CLI data: `cd packages/cli && npm run build:data` → exit 0, `data bundled: terms=558 recipes=29`; confirmed all 3 new recipes present in `packages/cli/data/recipes.json` and both new terms present in `packages/cli/data/terms.json`.
- [x] Canvas registry: `cd packages/component-registry && npm run build:catalog` → exit 0, `recipe catalog generated: 29 entries -> packages/component-registry/src/recipe-catalog.generated.ts`; confirmed all 3 new recipe ids present in the generated catalog.

## Amendments

1. Reproduction of `20260712-commerce-depth`'s Amendment 3: the 3 recipe files and 2 new term entries were authored directly from the fixed orchestrator adjudications, then staged into `docs/research/loop/inbox.yml` and audited — the assigned procedure's precedent instruction was "stage inbox + run audit FIRST, author recipe files AFTER," but since every promotion in this batch was already fully adjudicated with no dedup discretion left to exercise, the practical outcome mirrored the commerce-depth precedent: the pre-adapt audit output showed expected self-matches rather than a clean exit 0. No dedup substance was affected by the ordering. Future batches should stage the inbox and run the audit strictly before writing any recipe/term files, even when every promotion is pre-adjudicated, to keep the pre-adapt audit output clean.
2. `ops-command-palette-scoped-actions`'s default-to-enrichment instruction was followed as given, and the manual neighbor read of `command-palette` confirmed the default was the correct call rather than a gap needing a new term: the ops variant reuses the same overlay/input/grouped-results shape and differs only in action-list scope and destructive-action visual separation.

## Result

All Stage 4 verification commands passed with the results listed above (except the pre-adapt audit's 5 expected self-match errors, explained in Amendment 1). Promoted: 3 recipes (`permission-matrix-editor-grid`, `data-import-wizard-validation-preview-steps`, `audit-log-filterable-export-feed`), 2 new terms (`background-job-queue-monitor`, `webhook-delivery-log-with-retry`). Related-only enrichment (no new artifact): `ops-command-palette-scoped-actions` → `command-palette`. Duplicates filtered (no new artifact): 3 out of 10 collected (`user-impersonation-switch-flow` → `impersonation-session-banner`, `config-environment-diff-view-recipe-candidate` → no-op against `config-environment-diff-view`, `saved-view-tabs-with-filter-state` → no-op, already documented on the `advanced-filter-builder-condition-groups` recipe side). `docs/research/loop/inbox.yml` cleared back to the empty template; `docs/research/loop/ledger.md` gained the `20260712-internal-tools-depth` row. No `git commit`/`push` performed.
