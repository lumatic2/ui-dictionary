# changeset: VL2 step-1 — 용어 샤딩 생성기

- Date: 2026-07-21
- Plan: VL2 step-1 (`plans/2026-07-21-vl2-vocabulary-publication.md`)

## 무엇을 했나

`scripts/generate-vocabulary-shards.mjs` 신설. 665KB 원본을 **경량 인덱스 + 그룹별 샤드**
두 층으로 낸다. 통짜 배포는 컨텍스트의 상당량을 사전으로 채우고 정작 구현할 여력을 잃게 한다.

산출: 용어 562 → 샤드 58개, 인덱스 23.9KB(이름만·정의 없음), 최대 샤드 31KB(상한 40KB).

## 설계 판단

- **샤드 경계 = 그룹(57종)**. 이미 배포된 축이고 모든 용어가 갖고 있다. 새 분류를 발명하면
  `pattern-taxonomy.md`와 드리프트한다.
- **상한 초과 그룹은 하위 분할**. `data-tables-lists`가 44KB로 상한을 넘어 2조각이 됐다.
  조각 경계는 크기가 아니라 **정렬된 id**로 정한다 — 원본이 안 바뀌면 산출물도 안 바뀌어야
  URL이 흔들리지 않는다.
- **인덱스에 정의를 넣지 않는다.** 인덱스는 "무엇이 어디 사는지"만 답하고 본문은 샤드가 답한다.

## 검증

- 샤드 합집합 id = 원본 562 정확히 일치, 모든 샤드 상한 이하
- **failure probe**: group 없는 용어를 원본에 넣고 재실행 → `FAIL: group 이 없는 용어 1건` exit 1.
  조용한 누락은 사전에서 용어가 사라지는 최악의 실패라 명시 에러로 막았다.
- 상한 초과 시 조용히 넘어가지 않고 거부하는 것도 실제로 관측(첫 실행이 44KB로 실패)

## 곁가지 — 계수기 스캔 범위 확장

`audit-vocabulary-reach.mjs`가 최상위만 보고 있어 하위 디렉터리 샤드를 못 봤다. 재귀 1단계 추가.
그 결과 **끊긴 term_refs 91 → 0**. VL1이 예측한 대로("사유가 전부 미배포") 배포만으로 해소됐다.

## 판정

complete.
