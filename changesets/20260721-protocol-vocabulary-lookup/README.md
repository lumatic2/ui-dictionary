# changeset: VL6 step-1 — 프로토콜 용어 조회 단계

- Date: 2026-07-21 · Plan: VL6 step-1

`entry-protocol.md`에 **"Before the task branches"** 절 신설, 그 첫 단계가 N-1 용어 조회다.

VL1 계측: 이 문서의 `term`/`vocabulary`/`dictionary` 언급 **0회**. 사전이 배포돼도 부르는 자리가 없었다.

## 핵심 — 조회 실패가 작업 중단이 되면 안 된다

"사전에 없는 개념이면 `related` 한 겹만 따라보고 사전 밖으로 보고 진행하되 그 사실을 보고에 남긴다. **A lookup miss is never a reason to abandon the work.**" 사전이 못 덮는 자리가 어디인지가 다음 수집 배치의 입력이다.

원본 `terms.yml`을 찾아 나서지 말라는 것도 못 박았다 — 404가 정상이다.

## 검증

- VL1 갭 장부의 "여기서 용어 사전이 등장하는가" 항목이 전부 예로 바뀜
- 필수 단계 회귀 없음: `Always` 절(자가 판정·사람 확인)이 여전히 신설 절보다 앞(9행 vs 20행)
