# EVIDENCE — ECT4 이미지 노드·키 계약

- Milestone: ECT4 (`plans/2026-07-21-ect4-image-node-and-contract.md`)
- Horizon: 편집기에서 색이 색으로 보이고, 바꿔진다
- 완료일: 2026-07-21

## 이 milestone이 발견한 것 — **전제가 틀렸다**

ECT4 step-2는 "이미지 노드는 렌더러가 `tokenBindings`를 아예 참조하지 않으므로 확장한다"를
전제로 설계됐다. 착수 전 실측했더니 **전제가 거짓이었다.**

측정 (`CanvasSurface`를 실제로 렌더해 인라인 스타일을 읽음):

| 항목 | 값 |
|---|---|
| 이미지 노드 인라인 배경 | `oklch(0.95 0.015 270)` |
| `documentTokens().resolve('surface.muted')` | `oklch(0.95 0.015 270)` |
| **일치** | **true** |

`<img>`도 `shared.style`(=`nodeStyle`)을 받는다(`CanvasSurface.tsx:177`). 배경 바인딩은
처음부터 칠해지고 있었다.

### 이 틀린 문장이 어디까지 갔나

세션 첫 실사(코드 탐색)에서 나온 한 문장이 **네 곳으로 전파**됐다:

1. horizon 문서 "현 상태 실사" 2번 — 4표면 논거의 한 축
2. ECT3의 `bindableColorKeys`가 이미지 노드를 제외 — 사용자가 EU5에서 막힌 바로 그 노드
3. ECT4 step-2의 존재 이유 자체
4. **사용자 결정 3** ("이미지 노드: 렌더러까지 확장한다") — 사용자가 거짓 전제 위에서 선택했다

### EU5의 진짜 원인

사용자가 이미지 노드에서 색을 못 바꾼 이유는 렌더러가 아니라 **그 노드에 바인딩이 없었고
새로 묶을 경로도 없었던 것**이다. 그건 ECT3의 묶기 어포던스가 이미 해결한 문제다.
결정 3이 요구한 결과("묶으면 실제로 칠해진다")는 **이미 충족돼 있었다.**

### 왜 이제야 잡혔나

측정 방법이 바뀌어서다. 첫 실사는 **코드를 읽어** "image 분기에 tokenBindings가 안 보인다"고
판단했다. 실제로는 공통 `shared` 객체를 통해 흘러들어오고 있었고, 그건 읽어서는 놓치기 쉽다.
**렌더해서 재니 5분 만에 나왔다.**

## step-1 — 허용 키 정본 하나

같은 키 목록이 네 군데 따로 살고 있었다(template-core `EXPECTED_KIND`, 렌더러 하드코딩,
미해결 검사 배열, 인스펙터 라벨). 의존 방향(`template-core`·앱 → `canvas-core`)이
정본 자리를 정했다.

대조 게이트 두 겹: 계약 테스트, 그리고 **색 키에 사용자 언어 라벨이 없으면 개발 중 즉시 예외**.
라벨 없이 나가면 내부 키가 화면에 뜬다 — `Token · fill`이 EU5에서 정확히 그 형태였다.

probe로 정본에 `stroke`를 추가하면 계약 테스트 1건 + 인스펙터 예외가 함께 문다.

## step-2 — 이미지 노드를 연다

전제가 거짓이었으므로 "렌더러 확장"이 아니라 **틀린 전제로 막아둔 것을 열고, 실제로
칠해지는지 렌더 테스트로 고정**하는 일이 됐다.

| 항목 | 결과 |
|---|---|
| 이미지 노드에 묶기 어포던스가 나온다 | PASS |
| 배경 바인딩이 렌더에 반영 (캔버스와 같은 해석 함수 값) | PASS |
| 바인딩 없으면 이전과 동일 — 시각 회귀 없음 | PASS |
| 미해결 바인딩은 표시되고 폴백 색으로 안 덮임 | PASS |

**남은 사실 하나**: 불투명한 이미지는 자기 배경을 덮으므로 배경색이 눈에 안 보일 수 있다.
이건 결함이 아니라 겹침 순서의 당연한 결과이고, 투명 PNG·`contain` 맞춤에서는 보인다.

## 회귀 게이트

canvas-core 108 · template-core 195 · agent-design 199 · typecheck 0 · verify 0

## 배운 것

**코드를 읽어 내린 실사는 렌더해서 잰 것보다 약하다.** 이 horizon에서 측정이 판단을 뒤집은
게 세 번째다(어휘 이름 겹침, WebGPU 색, 이미지 렌더). 세 번 다 "읽어서 그렇다고 생각한 것"이
"재보니 아니었다"로 바뀌었다.

다음 horizon 설계의 실사 단계는 **가능하면 실행해서 재는 것**을 기본으로 한다.
