## 이어서 할 일
> 2026-08-01 세션 종료 시 기록 (워크트리 `발표-슬라이드-만드는-법` 대필)

- **워크트리 `발표-슬라이드-만드는-법` 병합 대기** — 브랜치 `lumatic2/발표-슬라이드-만드는-법` (`fcae518`) push 완료, main 미병합. 병합은 메인 세션 몫(사용자 확정). `/merge-worktree` 로 처리하되 사용자가 후속 작업용으로 워크트리 유지를 원할 수 있으니 정리 전 확인.
- **active goal 0** — 다음 방향은 `/harness-plan` 에서 개설. 대기 순서(2026-07-28): ② 『인터랙티브 웹 애니메이션』 자산화 ③ 사이트 품질(Get Started 빈 페이지·검색 결과 UI) ④ 다크모드 정비.
- 이월 finding: D2 Presenton 정밀 벤치 · 동영상 에셋 파이프(수요 미확인).
- 슬라이드 스킬 정본은 `~/projects/custom-skills/promoted/presentation-slides-yusung` — 배포본 직접 편집 금지, `setup.sh` 경유(커밋 가드가 강제).

### 현재 상태 / 주의점
- 이 main 체크아웃은 dirty(DESIGN.md·tokens·index.css 등 미커밋) — 다른 세션 작업일 수 있으니 건드리기 전 확인.
- goal `deck-quality` 완주 마감됨(ROADMAP goal-archive19, 보고서 `docs/reports/2026-08-01-dq3-canonical-redesign.md`) — 워크트리 브랜치에만 있고 main 에는 병합 전.
