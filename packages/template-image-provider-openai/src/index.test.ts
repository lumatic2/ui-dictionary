import{describe,expect,it}from'vitest';import{responseToAsset,serializeOpenAIImageRequest}from'./index.js';const req={id:'hero',role:'product',prompt:'handmade ceramic cup',width:1024,height:1024,fallbackUri:'./cup.svg'};describe('offline OpenAI adapter',()=>{it('serializes without network',()=>expect(serializeOpenAIImageRequest(req)).toEqual({model:'gpt-image-2',prompt:req.prompt,size:'1024x1024',output_format:'png'}));it('maps frozen response with provenance',()=>expect(responseToAsset(req,{b64_json:'AAAA',mimeType:'image/png',width:1024,height:1024,requestId:'fixture'}).provenance.source).toContain('fixture'));it.each([{b64_json:'',mimeType:'image/png',width:1024,height:1024},{b64_json:'x',mimeType:'image/jpeg',width:1024,height:1024},{b64_json:'x',mimeType:'image/png',width:512,height:512}])('rejects invalid response',response=>expect(()=>responseToAsset(req,response)).toThrow(/PROVIDER_/))})
