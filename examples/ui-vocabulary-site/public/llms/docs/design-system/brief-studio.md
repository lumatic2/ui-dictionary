# Brief Studio v2 — 실물 선택 브리프 (2단 구조)

Date: 2026-07-19 (v2 — 결정 공간 전면 확장, design-brief v2 §3과 쌍)
지위: 정본. design-brief.md의 시각층이 이 문서를 가리킨다. AskewlyDesign 데스크톱 앱 브리프 화면의 웹 프로토타입.

디자인 결정을 텍스트 라벨로 내리게 하지 마라 — 시각 결정은 **눈으로 보고 만져보고 고르는 것**이다. 풀 브리프의 시각층은 스튜디오가 기본 경로, 텍스트 질문은 폴백이다.

**v2 구조 (Style Tiles 관행)**: ① **1단 스타일 타일 3~4개** — 색+타이포+형태+질감 조합의 방향 카드(비주얼 키워드에서 파생). 선택 시 2단 후보가 그 방향으로 **추천 재정렬**된다(강제 필터 금지 — 타일 밖 조합 허용). ② **2단 축별 세부 14종** — design-brief v2 §3 표의 축 전부, **영향 큰 순서로 위→아래 배치**(베이스→액센트→타이포→이미지→구성→…), 그룹당 후보 **6~8개 추천순 정렬**(1순위 배지, 예약 상단 밴드 — 콘텐츠와 비겹침). 슬라이더 축(그리드·밀도, radius)은 3~5 스텝 프리셋.

**캐스케이드 + 라이브 미리보기 (ST2, 2026-07-19)**: 각 축의 후보는 **"지금까지의 선택"을 컨텍스트로 렌더**된다 — 배경을 고르면 액센트·타이포·카드 후보가 그 배경 위에서 다시 그려지고, 아직 안 고른 축은 추천 1순위를 임시값으로 쓴다(상위 재선택 시 하위 기선택은 보존). 우측 **스티키 라이브 미리보기 패널**에는 미니 사이트(헤더·히어로·카드·푸터)가 선택마다 실시간 조립된다 — 폭포를 내려가며 완성되는 자기 사이트를 보면서 고르는 구조. 좁은 뷰포트에서는 패널이 상단 고정 해제되어 흐름 앞에 배치된다.

**미리보기 뷰 토글 (SF3, 2026-07-19)**: 패널 타이틀 바의 **다크·모바일 토글**로 같은 선택 상태를 다크 서페이스·모바일 폭(세로 스택)으로 바꿔 본다 — 축 선택이 아니라 보기 방식이다. 다크/접근성 축과 연동: "라이트만" 선택 시 다크 토글은 비활성(사유 표시), "다크 우선" 선택 시 자동으로 다크 뷰가 된다(수동 토글이 우선). 다크 서페이스는 베이스 축의 다크 계열 후보에서 파생된다.

## 1. 에이전트 구동 절차

1. 템플릿 3파일 확보: `templates/brief-studio.html`(렌더러) + `templates/studio-data.default.json`(기준 데이터) + `templates/make-studio.py`(생성기), 서버는 `templates/brief-studio-server.py` (askewly 레포 로컬이 없으면 텍스트 인터뷰 폴백).
2. **후보 커스터마이즈 (SF1, 2026-07-19 — 데이터 주도)**: **HTML을 직접 편집하지 마라.** `studio-data.default.json`을 복사해 작업 폴더에 브리프 맥락(톤·비주얼 키워드)에 맞는 데이터 JSON을 만들고 — 타일 3~4(키워드 파생, `boost`로 추천 재정렬) · 축별 후보 6~8 `rank` 추천순 · 카피 축은 전략층 키워드 파생 문구, 이미지 축은 fetch-stock 산출 `bg: url(...)`/`video`/`poster`, 폰트 로딩은 최상위 `fonts`(Google Fonts CSS URL — typo 후보와 일치) — 1커맨드로 생성한다: `python templates/make-studio.py --data <data.json> --out <studio.html>`. 필수 필드·렌더러 필수 축이 빠지면 생성기가 명시 에러로 거부한다(조용한 폴백 없음). 렌더러 수정이 필요한 개선은 데이터가 아니라 템플릿 정본(`brief-studio.html`)에 하라.
3. `python brief-studio-server.py <port>` 구동 → **사용자 기본 브라우저로 열기** (Windows `Start-Process`, mac `open`).
4. `brief-selections.json` 생성을 폴링(백그라운드) — 사용자가 "선택 완료"를 누르면 생성된다.
5. 선택값을 DESIGN.md(공식 Stitch 스키마)로 매핑: 팔레트 → `colors:` 실값, 폰트 → `typography.*.fontFamily`, 인터랙션 수위 → Do's and Don'ts·모션 프로즈 + expressive-stack 티어 상한.
6. 확인 후 서버 종료·임시 파일 정리.

## 2. 폴백 (스튜디오 실패 ≠ 브리프 중단)

- **서버 미가동/전송 실패**: 페이지가 선택 JSON을 textarea로 표시 — 사용자가 채팅에 붙여넣으면 동일하게 처리한다.
- **오프라인(폰트 fetch 실패)**: 스펙시멘이 시스템 폰트로 렌더 — 페이지는 깨지지 않으며, 서체 도메인만 텍스트 질문으로 전환한다.
- **브라우저를 못 여는 환경(headless)**: design-brief.md §4 폴백(추천 기본값 + 추정 명시) 적용.

## 3. 후보 큐레이션 규칙

- 후보는 무작위가 아니라 **전략층 답변(톤·비주얼 키워드)의 파생**이다 — 톤이 "따뜻함"이면 온도 변주를 보여라. 1단 타일도 키워드 3~4개에서 파생.
- 팔레트·배경 후보는 반드시 같은 미니 UI(제목·보조 텍스트·버튼·카드)에 입혀 비교 가능하게 — 스와치 나열 금지. 배경(베이스)과 액센트는 **독립 그룹**으로 조합 선택.
- 타이포는 헤딩/본문 **페어 단위**로. 헤더·푸터·카드·구성은 실물 미니 렌더/와이어프레임으로. 이미지 스타일은 실물 썸네일로(스톡·생성 — 이미지 절 참조).
- 모션 "풍부" 선택 시에도 실험적 표현(③·④ 티어)은 별도 수동 승인 원칙 유지 (expressive-stack 판정 절차 5).
- 아이콘 후보에 이모지 금지(시그니처 비선호), 접근성 기본 추천은 AA.

## 3.5 구성 축 — 12유형 카탈로그 + 조건 노출 (SF2, 2026-07-19)

- 구성 후보 카탈로그는 리서치 12유형 전량이다 (`research/2026-07-19-st4-composition-patterns.md`): 표준 퍼널 · Z-패턴 스플릿 · 풀블리드 미디어 · 스텝 온보딩 · 소셜프루프 선행 · 롱폼 스토리 · 원스크린 · 갤러리 우선 + **비교표 · 문제-메커니즘-증거 · 커뮤니티 UGC · FAQ 신뢰** (신규 4종은 기본 `hidden`).
- **조건 노출**: 기본 노출은 8종 이하 — 브리프 맥락이 맞을 때만 데이터 JSON에서 해당 후보의 `hidden`을 빼고 노출한다(과밀 = 선택 피로). 판정 근거: 회의적/전환 시장 → 비교표, 신개념·설명 필요 제품 → 문제-메커니즘-증거, 커뮤니티·리뷰 자산 보유 → UGC, 고관여·고가 서비스 → FAQ 신뢰. 노출을 늘리면 다른 후보를 빼서 8종 이하를 유지하는 것을 권장한다.
- **예약/티켓형 특수 패턴** (`booking-widget` 후보 — 예약·티켓·숙박·공연 업종 일치 시만 노출): ① 예약 위젯(날짜·인원·CTA)이 별도 섹션이 아니라 **히어로 안에 통합**된다 ② 재고·좌석·시간대가 추상 단계 안내 대신 **실물 단위로 시각화**된다(잔여 슬롯·좌석 맵·가용 캘린더) ③ 긴급 신호(잔여 수량·카운트다운)는 CTA 옆에 붙인다. **긴급 신호 진실성(의무)**: 잔여 수량·마감 표시는 실재 데이터가 있는 경우에만 구현한다 — 가짜 희소성은 다크패턴이다(리서치 §3, NBC 보도 근거).
- **구성→구현 출발점**: 구성 후보의 `pv` 섹션마다 `recipes` 매핑이 필수다. 제출 payload는 `implementation: { composition, recipes }`를 포함한다. 소비 순서는 ① `/r/<recipe>.json` 존재 시 registry 코드 이식 ② 프로젝트 DESIGN.md 토큰으로 재결합 ③ 코드 자산이 없으면 `/llms/recipes/**/<recipe>.md` 문서-재구현 폴백 ④ recipe Checks 실행이다. `make-studio.py`는 `pv`와 `recipes` 키가 하나라도 다르면 생성을 거부한다.

## 4. 이미지 축 — 실사 파이프라인 (스톡 우선 + 생성 옵션)

- **스톡(기본)**: `templates/fetch-stock.py`로 Pexels에서 후보 6~8장을 가져온다 — `locale=ko-KR` 한국어 쿼리, 쿼리는 전략층 답변(업종·톤·키워드)에서 파생. **해상도 계약(ST1)**: 스튜디오 썸네일은 `cand-N.jpg`(medium), **최종 페이지에는 반드시 `cand-N-full.jpg`(large2x, 부재 시 large→original 폴백)** — medium을 최종 페이지에 쓰는 것은 결함이다(2026-07-19 실연 적발). 환경변수 `PEXELS_API_KEY`(User 전역, 2026-07-19 등록 — 값 출력·커밋·로그 금지). 다운로드된 썸네일을 데이터 JSON의 imagery 후보 `bg: url(...)`에 넣어 make-studio.py로 주입한다. `candidates.json`의 `avg_color`로 선택된 베이스/액센트와 톤이 맞는 후보를 우선 정렬할 수 있다. photographer·pexels_url 메타 보존(라이선스: Pexels 무료·상업 가능, 표기 권장).
- **영상(ST3)**: `fetch-stock.py --video "쿼리"` — Pexels Videos(`locale=ko-KR`, 동일 키)에서 **HD(≤1280px) mp4 + 포스터**를 받는다(4K 원본 회피, 15MB 이하 후보 우선). 스튜디오 이미지 축에 영상 후보(`video`/`poster` 필드)를 섞으면 hover 시 muted 재생으로 미리본다. **히어로 비디오 패턴**: `<video autoplay muted playsinline poster>` + `prefers-reduced-motion`·모바일(협대역)에서는 자동재생 없이 포스터 폴백 의무. 크리에이터 크레딧 보존(로테이션 시 전원). **영상 로테이션 규칙(사용자 확정 2026-07-19)**: 짧은 클립 하나를 `loop`로 돌리면 반복이 어색하다 — **최소 3종의 영상을 `ended` 이벤트로 순환**시켜 자연스러운 흐름을 만든다(같은 쿼리는 동일 결과가 나오니 각도가 다른 쿼리 2~3개로 수집).
- **생성(옵션)**: 맞춤 이미지가 필요하면 ① `/comfy`(로컬 ComfyUI — 무료·무제한·시드 고정) ② Codex 내장 image_gen(클라우드 단발). 생성 후보를 스톡 후보와 같은 그룹에 섞어 제시할 수 있다.
- **폴백**: 키 부재·오프라인·율리밋(200/h) 시 헬퍼가 명시 에러를 내고, 스튜디오는 톤 카드(그라디언트)로 degrade — 페이지 불괴.

## Changelog

- 2026-07-19: RC3 — 구성 13항의 섹션→recipe 매핑, 제출 payload 노출, 코드 자산/문서 폴백 소비 계약.
- 2026-07-19: SF3 — 미리보기 다크·모바일 뷰 토글 (다크 축 연동).
- 2026-07-19: SF2 — 구성 12유형 카탈로그 + 조건 노출(`hidden`) + 예약형 특수 패턴·긴급 신호 진실성 (§3.5).
- 2026-07-19: SF1 — 후보 주입을 데이터 JSON + `make-studio.py` 1커맨드로 전환 (HTML 직접 편집 금지).
- 2026-07-19: v2 — 2단 구조·축 14종·이미지 파이프라인 절(SP1·SP2·SP3).
- 2026-07-19: 초판 (VB2 — 사용자 발의: "폰트·컬러·인터랙션을 눈으로 보고 선택").
