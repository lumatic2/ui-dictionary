# UI Vocabulary — 용어 인덱스

> 용어 562개 / 그룹 58개. **이름만 있고 정의는 없다** — 정의는 그룹 샤드에 있다.
> 조회: 여기서 용어를 찾아 그 그룹 샤드 하나만 가져가라. 원본 전체(680KB)를 받을 일은 없다.
> 파생물 — 정본은 `docs/ui-vocabulary/terms.yml`.

## accessibility

### ARIA·스크린리더 — `accessibility-aria-screen-reader.yml` (4)

aria-expanded(확장 상태 ARIA), aria-invalid(오류 입력 ARIA), aria-live(라이브 영역 ARIA), sr-only(스크린리더 전용 텍스트)

### 포커스·움직임 접근성 — `accessibility-focus-motion.yml` (4)

color-contrast(색 대비), direction-provider(방향성 제공자), focus-trap(포커스 트랩), reduced-motion(모션 줄이기)

## action

### 대량·위험 작업 — `action-bulk-danger.yml` (6)

bulk-action-bar(일괄 작업 바), destructive-confirmation(파괴적 작업 확인), grab-handle(잡기 핸들), swipe-action(스와이프 액션), swipe-action-row(스와이프 액션 행), swipe-to-delete(밀어서 삭제)

### 버튼·아이콘 행동 — `action-buttons.yml` (24)

bottom-app-bar(하단 앱 바), bottom-cta-bar(하단 CTA 바), button(버튼), button-group(버튼 그룹), clear-text-button(텍스트 지우기 버튼), close-button(닫기 버튼), copy-button(복사 버튼), cta-section(CTA 섹션), destructive-button(위험 버튼), download-button(다운로드 버튼), floating-action-button(플로팅 버튼), floating-search-button(플로팅 검색 버튼), icon-button(아이콘 버튼), icon-toggle-button(아이콘 토글 버튼), link(링크), loading-button(로딩 버튼), overflow-button(더보기 버튼), primary-button(주요 버튼), secondary-button(보조 버튼), share-button(공유 버튼), social-login-button-group(소셜 로그인 버튼 그룹), speed-dial(스피드 다이얼), split-button(스플릿 버튼), voice-input-button(음성 입력 버튼)

### 툴바·커맨드 — `action-command-bars.yml` (7)

action-sheet(액션 시트), canvas-toolbar(캔버스 툴바), command-bar(커맨드 바), menu-item(메뉴 항목), share-sheet(공유 시트), table-row-actions(테이블 행 액션), toolbar(툴바)

### 편집·미디어 제어 — `action-editor-media.yml` (5)

audio-player-controls(오디오 플레이어 컨트롤), cropper(크롭퍼), pinch-zoom-viewer(핀치 줌 뷰어), scrubber(스크러버), video-player-controls(비디오 플레이어 컨트롤)

### 기타 행동 — `action-misc.yml` (1)

reorder-handle(순서 변경 핸들)

## data-display

### 계정·지원 정보 — `data-account-support.yml` (3)

keyboard-shortcut-key(키보드 단축키 키), mfa-enrollment-card(2단계 인증 등록 카드), recovery-code-panel(복구 코드 패널)

### 기본 콘텐츠 요소 — `data-basic-content-elements.yml` (11)

accordion(아코디언), carousel(캐러셀), divider(구분선), icon(아이콘), image(이미지), item-row(아이템 행), label(라벨), logo(로고), spacer(여백), thumbnail(썸네일), typography(타이포그래피)

### 채팅·메시지 — `data-chat-messaging.yml` (4)

chat-attachment(채팅 첨부 파일), chat-bubble(채팅 말풍선), chat-message(채팅 메시지), notification-inbox-row(알림 인박스 행)

### 커머스·청구 — `data-commerce-billing.yml` (9)

address-card(주소 카드), attachment-list(첨부파일 목록), billing-summary(결제 요약), cart-summary(장바구니 요약), cart-summary-bar(장바구니 요약 바), feature-comparison(기능 비교), image-gallery(이미지 갤러리), invoice-row(인보이스 행), reviews-summary-block(리뷰 요약 블록)

### 기타 데이터·콘텐츠 — `data-display-misc.yml` (23)

blog-post-card(블로그 포스트 카드), calendar-event-card(캘린더 이벤트 카드), calendar-view(캘린더 뷰), contact-info-panel(연락처 정보 패널), description-list(설명 목록), docs-changelog-page(체인지로그 페이지), faq-list(FAQ 목록), featured-post-card(대표 포스트 카드), feed-card-mobile(모바일 피드 카드), file-card(파일 카드), help-center-card(도움말 센터 카드), kanban(칸반), kanban-card(칸반 카드), media-card(미디어 카드), office-location-card(오피스 위치 카드), onboarding-checklist(온보딩 체크리스트), plan-card(플랜 카드), price-card(가격 카드), product-card(상품 카드), profile-card(프로필 카드), release-note-card(릴리즈 노트 카드), story-rail(스토리 레일), story-viewer(스토리 뷰어)

### 지표·차트 — `data-metrics-charts.yml` (16)

area-chart-card(영역 차트 카드), bar-chart-card(막대 차트 카드), chart(차트), chart-axis(차트 축), chart-kpi-card(KPI 차트 카드), chart-tooltip-pattern(차트 툴팁 패턴), interactive-chart-card(인터랙티브 차트 카드), legend(범례), line-chart-card(선 차트 카드), map-marker(지도 마커), metric-card(지표 카드), pie-chart-card(파이 차트 카드), radar-chart-card(레이더 차트 카드), radial-chart-card(방사형 차트 카드), stacked-chart-card(누적 차트 카드), stat-list(지표 리스트)

### 움직이는 콘텐츠 — `data-motion-content.yml` (2)

infinite-moving-cards(무한 이동 카드), number-ticker(숫자 티커)

### 사람·연동 — `data-people-integrations.yml` (11)

avatar(아바타), avatar-group(아바타 그룹), avatar-stack(아바타 스택), badge-group(배지 그룹), connection-card(연결 카드), integration-card(연동 카드), integration-grid-section(연동 그리드 섹션), status-avatar(상태 아바타), team-member-row(팀원 행), webhook-delivery-log-with-retry(웹훅 전송 로그), webhook-endpoint-row(웹훅 엔드포인트 행)

### 테이블·리스트 1조각 — `data-tables-lists-1.yml` (18)

advanced-filter-builder(고급 필터 빌더), api-error-reference-table(API 에러 레퍼런스 테이블), column-header-menu(컬럼 헤더 메뉴), column-visibility-menu(컬럼 표시 메뉴), comparison-table(비교 테이블), config-environment-diff-view(환경 설정 비교 뷰), contained-list(컨테인드 리스트), data-grid(데이터 그리드), data-table-toolbar(데이터 테이블 툴바), detail-row(상세 행), drag-to-reorder-list(드래그 순서 변경 목록), draggable-list(드래그 리스트), expandable-row(펼침 행), faceted-filter(패싯 필터), filter-bar(필터 바), filter-panel(필터 패널), grouped-list(그룹 리스트), infinite-scroll(무한 스크롤)

### 테이블·리스트 2조각 — `data-tables-lists-2.yml` (17)

list(리스트), nested-list(중첩 리스트), order-history-list(주문 내역 목록), permission-matrix-editor(권한 매트릭스 편집기), pivot-table(피벗 테이블), pull-to-refresh(당겨서 새로고침), query-builder(쿼리 빌더), review-list-with-filters(필터 리뷰 목록), selection-summary(선택 요약), sort-control(정렬 컨트롤), structured-list(구조화 목록), table(테이블), table-density-control(테이블 밀도 조절), tree-table(트리 테이블), tree-view(트리 뷰), virtualized-list(가상화 리스트), wishlist-management-view(위시리스트 관리 화면)

### 활동·히스토리 — `data-timeline-history.yml` (6)

activity-feed(활동 피드), audit-log(감사 로그), background-job-queue-monitor(백그라운드 작업 큐 모니터), notification-list(알림 목록), timeline(타임라인), version-history-list(버전 히스토리 목록)

## feedback

### 권한·제한·세션 — `feedback-access-limits.yml` (12)

biometric-prompt(생체 인증 프롬프트), impersonation-session-banner(임퍼스네이션 세션 배너), locked-state(잠금 상태), offline-state(오프라인 상태), permission-education-screen(권한 안내 화면), permission-prompt(권한 요청 프롬프트), permission-state(권한 필요 상태), quota-warning(사용량 경고), rate-limit-state(사용량 제한 상태), session-expired-dialog(세션 만료 다이얼로그), unsaved-changes-banner(저장 안 된 변경 배너), upgrade-prompt(업그레이드 유도)

### 동기화·예측 상태 — `feedback-accessibility-states.yml` (3)

gesture-hint(제스처 힌트), optimistic-update(낙관적 업데이트), pending-state(대기 상태)

### 알림·토스트·배너 — `feedback-alerts-toasts.yml` (16)

alert(알림 배너), announcement-banner(공지 배너), callout(콜아웃), error-toast(오류 토스트), inline-alert(인라인 알림), message-bar(메시지 바), mobile-alert-dialog(모바일 경고 다이얼로그), mobile-snackbar(모바일 스낵바), mobile-toast(모바일 토스트), snackbar(스낵바), success-toast(성공 토스트), toast(토스트), toast-stack(토스트 스택), touch-ripple(터치 리플), trial-banner(체험판 배너), undo-toast(실행 취소 토스트)

### 인증·보안 상태 — `feedback-auth-security.yml` (14)

access-pending-state(접근 승인 대기 상태), access-request-panel(접근 요청 패널), device-approval-state(기기 승인 대기 상태), email-verification-banner(이메일 인증 배너), invite-expired-state(초대 만료 상태), magic-link-sent-state(매직 링크 전송 상태), passkey-enrollment-prompt(패스키 등록 유도), passkey-sign-in-sheet(패스키 로그인 시트), reconnect-account-state(계정 재연결 상태), recovery-code-warning(복구 코드 경고), setup-blocker-state(설정 차단 상태), trusted-device-prompt(신뢰 기기 등록 프롬프트), verification-required-screen(인증 필요 화면), workspace-join-request(워크스페이스 참여 요청)

### 확인·도움말 — `feedback-confirmation-help.yml` (3)

confirmation-dialog(확인 다이얼로그), docs-feedback-widget(문서 피드백 위젯), tooltip(툴팁)

### 빈 상태·오류 복구 — `feedback-empty-error.yml` (14)

empty-filter-state(필터 결과 없음 상태), empty-search-result(검색 결과 없음), empty-state(빈 상태), empty-table(빈 테이블), error-boundary(오류 경계 화면), error-state(오류 상태), info-state(정보 상태), location-permission-empty(위치 권한 빈 상태), maintenance-state(점검 상태), mobile-empty-feed(모바일 빈 피드), retry-panel(재시도 패널), success-state(성공 상태), validation-message(검증 메시지), warning-state(경고 상태)

### 인터랙션 상태 — `feedback-interaction-states.yml` (9)

active-state(활성 상태), disabled-state(비활성 상태), drag-state(드래그 상태), drop-target(드롭 대상), focus-ring(포커스 링), hover-reveal(호버 리빌), hover-state(호버 상태), selected-state(선택 상태), tap-feedback(탭 피드백)

### 로딩·진행 — `feedback-loading-progress.yml` (9)

inline-loading(인라인 로딩), loading-state(로딩 상태), progress-bar(진행바), saving-indicator(저장 표시기), setup-progress(설정 진행률), skeleton(스켈레톤), skeleton-table(테이블 스켈레톤), spinner(로딩 스피너), syncing-state(동기화 중 상태)

### 기타 상태·피드백 — `feedback-misc.yml` (8)

blur-fade-in(블러 페이드 인), duration(지속 시간), easing(이징), fade-transition(페이드 전환), scale-transition(스케일 전환), slide-transition(슬라이드 전환), transition(전환), typing-text-effect(타이핑 텍스트 효과)

### 상태·알림 센터 — `feedback-status-notifications.yml` (18)

badge(배지), checkout-progress-header(결제 진행 헤더), coach-mark(코치마크), connection-status(연결 상태), conversation-marker(대화 마커), delivery-tracker(배송 추적), health-indicator(헬스 인디케이터), hover-card(호버 카드), info-label(정보 라벨), meter(미터), notification-center(알림 센터), order-status(주문 상태), password-strength-meter(비밀번호 강도 미터), progress-stepper(진행 스테퍼), role-badge(역할 배지), sonner-toast(소너 토스트), status-chip(상태 칩), status-indicator(상태 표시)

## input

### 계정·설정 폼 — `input-auth-forms.yml` (20)

account-creation-form(계정 생성 폼), change-password-form(비밀번호 변경 폼), email-change-form(이메일 변경 폼), forgot-password-form(비밀번호 찾기 폼), formisch-form-pattern(Formisch 폼 패턴), invite-signup-form(초대 가입 폼), login-form(로그인 폼), magic-link-login-form(매직 링크 로그인 폼), mfa-challenge-form(2단계 인증 폼), newsletter-section(뉴스레터 섹션), otp-challenge-form(OTP 인증 폼), passkey-login-form(패스키 로그인 폼), popover-form(팝오버 폼), profile-settings-form(프로필 설정 폼), react-hook-form-pattern(React Hook Form 패턴), reauthentication-form(재인증 폼), reset-password-form(비밀번호 재설정 폼), signup-form(회원가입 폼), sso-login-form(SSO 로그인 폼), tanstack-form-pattern(TanStack Form 패턴)

### 인라인 편집 — `input-editing.yml` (2)

inline-edit(인라인 편집), rich-text-editor(리치 텍스트 에디터)

### 파일·미디어 입력 — `input-file-media.yml` (2)

file-upload(파일 업로드), upload-dropzone(업로드 드롭존)

### 선택형 입력 — `input-pickers.yml` (22)

chip-input-mobile(모바일 칩 입력), color-picker(색상 선택기), combobox(콤보박스), contact-picker(연락처 선택), coupon-field(쿠폰 입력 필드), date-picker(날짜 선택), date-preset-picker(날짜 프리셋 선택기), date-range-picker(날짜 범위 선택), mobile-date-picker(모바일 날짜 선택), mobile-date-time-picker-sheet(모바일 날짜·시간 선택 시트), native-select(네이티브 선택창), otp-input(인증번호 입력), product-option-sheet(상품 옵션 시트), pull-to-refresh-indicator(당겨서 새로고침 표시), quantity-stepper(수량 스테퍼), rating-input(평점 입력), select(선택창), tag-input(태그 입력), tag-picker(태그 선택기), time-picker(시간 선택), time-wheel-picker(시간 휠 피커), wheel-picker(휠 피커)

### 검색·명령 입력 — `input-search-command.yml` (8)

address-autocomplete(주소 자동완성), autocomplete(자동완성), command-palette(커맨드 팔레트), mobile-search-header(모바일 검색 헤더), mobile-search-sheet(모바일 검색 시트), search-field(검색창), search-suggestions(검색 제안), search-view(검색 뷰)

### 텍스트·숫자 입력 — `input-text.yml` (20)

api-key-field(API 키 필드), character-count-field(글자 수 카운터 필드), clearable-input(지우기 버튼 입력창), comment-composer(댓글 작성 바), copy-field(복사 필드), field-group(필드 그룹), fieldset(필드셋), floating-label-field(플로팅 라벨 필드), inline-submit-field(인라인 제출 필드), input-group(입력 그룹), masked-input(마스크 입력), mobile-form-field-stack(모바일 폼 필드 스택), number-input(숫자 입력창), otp-code-input(OTP 코드 입력), passcode-keypad(패스코드 키패드), password-field(비밀번호 입력창), spin-button(스핀 버튼 입력), text-field(입력창), textarea(텍스트영역), textarea-autosize(자동 높이 텍스트영역)

## layout-rendering

### 반응형·뷰포트 — `layout-responsive-viewport.yml` (7)

breakpoint(브레이크포인트), container-query(컨테이너 쿼리), dynamic-viewport-unit(동적 뷰포트 단위), fluid-layout(유동 레이아웃), mobile-first(모바일 우선), responsive-layout(반응형 레이아웃), safe-area-inset(안전 영역 인셋)

### 스크롤 동작 — `layout-scroll-behavior.yml` (1)

scroll-snap(스크롤 스냅)

### 간격·크기 — `layout-spacing-sizing.yml` (4)

aspect-ratio(종횡비), gap(갭), object-fit(오브젝트 맞춤), padding(패딩)

### 겹침·넘침 — `layout-stacking-overflow.yml` (6)

clipping-mask(클리핑 마스크), layout-shift(레이아웃 이동), overflow(오버플로), stacking-context(스태킹 컨텍스트), sticky-position(스티키 위치), z-index(Z-인덱스)

## selection

### 계정·결제 컨텍스트 — `selection-context.yml` (4)

account-switcher(계정 전환기), payment-method-card(결제수단 카드), versioned-docs-switcher(문서 버전 스위처), workspace-switcher(워크스페이스 전환기)

### 메뉴·드롭다운 — `selection-menus.yml` (7)

context-menu(컨텍스트 메뉴), dropdown-menu(드롭다운 메뉴), long-press-menu(길게 누르기 메뉴), mega-menu(메가 메뉴), menu-bar(메뉴바), mobile-popover-menu(모바일 팝오버 메뉴), saved-view-tabs(저장된 뷰 탭)

### 기타 선택·탐색 — `selection-misc.yml` (1)

row-selection(행 선택)

### 탐색 전환 — `selection-navigation.yml` (21)

anchor-nav(앵커 내비게이션), back-button(뒤로가기 버튼), bottom-navigation(하단 내비게이션), breadcrumb(브레드크럼), breadcrumbs-menu(브레드크럼 메뉴), data-import-wizard-validation-preview(데이터 임포트 검증 위저드), edge-swipe-back(가장자리 스와이프 뒤로가기), mobile-segmented-tabs(모바일 세그먼트 탭), navigation-bar(내비게이션 바), navigation-menu(내비게이션 메뉴), navigation-rail(내비게이션 레일), onboarding-carousel-skip-pattern(건너뛰기 있는 온보딩 페이저), onboarding-pager(온보딩 페이저), page-control(페이지 컨트롤), pagination(페이지네이션), scope-bar(스코프 바), sidebar-nav(사이드바 내비게이션), stepper(스테퍼), tab-bar(탭 바), tree-navigation(트리 내비게이션), wizard(위저드)

### 옵션 선택 — `selection-options.yml` (21)

auth-method-choice(인증 방식 선택), checkbox(체크박스), checkbox-card(체크박스 카드), chip(칩), content-tabs(콘텐츠 탭), disclosure(펼침 버튼), filter-chip(필터 칩), import-data-choice(데이터 가져오기 선택), inline-date-range-chip(인라인 날짜 범위 칩), listbox(리스트박스), multi-select(다중 선택창), radio-group(라디오그룹), range-slider(범위 슬라이더), segmented-control(세그먼트 버튼), slider(슬라이더), switch(스위치), tabs(탭), toggle-button(토글 버튼), toggle-group(토글 그룹), transfer-list(전송 리스트), variant-selector-buttons(변형 선택 버튼 그룹)

## structure

### 앱·페이지 레이아웃 — `structure-app-layout.yml` (16)

app-shell(앱 셸), billing-settings-page(결제 설정 페이지), breadcrumb-header(브레드크럼 헤더), checkout-step(체크아웃 단계), dashboard-grid(대시보드 그리드), dashboard-overview-page(대시보드 개요 페이지), footer(푸터), global-header(전역 헤더), header(헤더), onboarding-flow-page(온보딩 플로우 페이지), page-layout(페이지 레이아웃), page-title-bar(페이지 제목 바), settings-page-layout(설정 페이지 레이아웃), status-bar(상태 표시줄), top-app-bar(상단 앱 바), utility-bar(유틸리티 바)

### 인증·온보딩 화면 — `structure-auth-layouts.yml` (7)

auth-card(인증 카드), consent-review-screen(동의 검토 화면), invite-acceptance-screen(초대 수락 화면), login-dialog(로그인 다이얼로그), login-page(로그인 페이지), split-auth-layout(분할 인증 레이아웃), welcome-choice-screen(시작 선택 화면)

### 콘텐츠 구조 요소 — `structure-content-elements.yml` (4)

aspect-ratio-box(비율 고정 박스), interactive-api-playground(인터랙티브 API 플레이그라운드), message-scroller(메시지 스크롤러), scroll-area(스크롤 영역)

### 기타 구조 — `structure-misc.yml` (3)

favicon(파비콘), open-graph-image(공유 미리보기 이미지), testimonial-section(후기 섹션)

### 반응형·모바일 — `structure-mobile.yml` (15)

carousel-peek(피크 캐러셀), full-screen-dialog(전체 화면 다이얼로그), large-title-header(큰 제목 헤더), map-bottom-panel(지도 하단 패널), media-lightbox-mobile(모바일 미디어 라이트박스), mobile-app-bar(모바일 앱 바), mobile-bottom-sheet(모바일 바텀 시트), mobile-filter-bottom-sheet(모바일 필터 바텀 시트), mobile-status-bar(모바일 상태 바), modal-bottom-sheet(모달 바텀 시트), responsive-stack(반응형 스택), safe-area(안전 영역), scrim(스크림), sheet-drag-handle(시트 드래그 핸들), standard-bottom-sheet(표준 바텀 시트)

### 사이드바·보드 구조 — `structure-navigation.yml` (10)

board-column(보드 컬럼), breadcrumb-overflow(축약 브레드크럼), collapsible-sidebar(접이식 사이드바), floating-navbar(플로팅 내비게이션 바), mini-sidebar(미니 사이드바), nav-user-menu(사용자 메뉴), navbar-menu(내비게이션 메뉴), navigation-drawer(내비게이션 드로어), pagination-jump(페이지 점프 입력), sidebar-section(사이드바 섹션)

### 패널·오버레이 — `structure-panels.yml` (18)

adaptive-split-view(적응형 스플릿 뷰), card(카드), dialog(모달), disclosure-card(펼침 카드), dockable-panel(도킹 패널), drawer(드로어), floating-panel(플로팅 패널), inspector-panel(인스펙터 패널), lightbox(라이트박스), master-detail(마스터-디테일), popover(팝오버), preview-pane(미리보기 패널), product-quickview-modal(상품 퀵뷰 모달), properties-panel(속성 패널), resizable-panel(크기 조절 패널), right-rail(우측 레일), side-sheet(사이드 시트), split-pane(분할 패널)

### 섹션·콘텐츠 구조 — `structure-sections.yml` (21)

bento-grid(벤토 그리드), blog-grid-section(블로그 그리드 섹션), contact-form-section(문의 폼 섹션), container(컨테이너), content-section(콘텐츠 섹션), cross-sell-related-strip(연관 상품 추천 스트립), danger-zone(위험 구역), disclosure-group(디스클로저 그룹), docs-landing-hub-category-cards(문서 랜딩 허브 카드), feature-grid-section(기능 그리드 섹션), grid(그리드), hero(히어로 영역), incentive-trust-strip(혜택·신뢰 스트립), logo-cloud-section(로고 클라우드 섹션), pricing-section(가격 섹션), section(섹션), split-content-section(분할 콘텐츠 섹션), spotlight-hero(스포트라이트 히어로), sticky-footer-bar(고정 하단 바), sticky-header(고정 헤더), sticky-scroll-section(스티키 스크롤 섹션)

### 사이드바 레이아웃 — `structure-sidebar-layouts.yml` (9)

calendar-sidebar-layout(캘린더 사이드바 레이아웃), collapsible-sidebar-layout(접이식 사이드바 레이아웃), dual-sidebar-layout(양쪽 사이드바 레이아웃), file-tree-sidebar-layout(파일 트리 사이드바 레이아웃), icon-sidebar-layout(아이콘 사이드바 레이아웃), inset-sidebar-layout(인셋 사이드바 레이아웃), right-sidebar-layout(오른쪽 사이드바 레이아웃), sidebar-dashboard-layout(사이드바 대시보드 레이아웃), sidebar-dialog-layout(다이얼로그 안 사이드바 레이아웃)

## style

### 선·색상 처리 — `style-border-color.yml` (8)

blend-mode(블렌드 모드), border-radius(모서리 반경), gradient-fill(그라디언트 채움), hairline-border(헤어라인 보더), highlight-stroke(하이라이트 스트로크), opacity(투명도), outline(아웃라인), semantic-color(의미 색상)

### 장식·배경 효과 — `style-decorative-effects.yml` (14)

animated-gradient-background(애니메이션 그라디언트 배경), aurora-background(오로라 배경), background-beams(배경 빔), border-beam(보더 빔), canvas-reveal-card(캔버스 리빌 카드), dot-pattern-background(점 패턴 배경), grid-pattern-background(그리드 패턴 배경), marquee-row(마키 행), orbiting-icons(궤도 아이콘), scroll-fade(스크롤 페이드), shimmer-effect(쉬머 효과), spotlight-card(스포트라이트 카드), three-d-card(3D 카드), tracing-beam-section(트레이싱 빔 섹션)

### 기타 스타일·재질 — `style-misc.yml` (1)

hover-card-stack(호버 카드 스택)

### 표면·재질 — `style-surface-material.yml` (6)

backdrop-blur(백드롭 블러), glassmorphism(글래스모피즘), inner-shadow(내부 그림자), noise-texture(노이즈 텍스처), shadow-elevation(그림자 깊이), translucent-surface(반투명 표면)

### 토큰·시맨틱 — `style-tokens.yml` (1)

theme-token(테마 토큰)

### 타이포그래피 처리 — `style-typography.yml` (6)

animated-shiny-text(샤이니 텍스트), font-weight(글자 굵기), letter-spacing(자간), line-clamp(줄 수 제한), line-height(줄 높이), text-overflow(텍스트 넘침 처리)
