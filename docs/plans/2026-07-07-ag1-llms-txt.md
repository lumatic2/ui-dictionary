# Plan - AG1 llms.txt 발견 계층

Date: 2026-07-07
Status: draft (승인 대기)

## 위계

- Objective: `docs/OBJECTIVE.md` (장기 아크 5 — 에이전트 통합)
- Horizon: `docs/horizons/2026-07-agent-integration.md` (active)
- Milestone: ROADMAP `AG1` — llms.txt 발견 계층

## Scope

이번 run이 닫는 범위: `ui.askewly.com/llms.txt` 생성 파이프라인 + 사이트 서빙 + 실배포 검증까지. 제외: MCP 서버(C-11), 레시피 확장(AG3 수요 주도), 스킬 연결(AG2).

중단점: ① Step 2 push는 사용자 승인 후에만(CLAUDE.md 배포 규칙) ② 검증 FAIL 시 blocked 보고 ③ AG1 완료 후 horizon-run 연쇄로 AG2 진행.

## Step 트리

- [ ] Step 1 — 생성 파이프라인 + 사이트 통합: `scripts/generate-llms-txt.mjs`가 SSOT(tokens/askewly.tokens.json, pattern-taxonomy.md, groups.yml, recipe-format.md, recipes/ 5종, agent-asset-model.md)에서 `llms.txt`(링크만, 값 중복 금지 — C-10)와 raw 자산 사본을 사이트 `public/llms/`로 파생 + 링크 무결성 검사 (verify: `node scripts/generate-llms-txt.mjs` 무결성 PASS + `npm run build` 후 dist에서 `/llms.txt`·표본 자산 fetch 200)
- [ ] Step 2 — 배포 + 실배포 검증: push(사용자 승인 게이트) → 자동 배포 후 `curl https://ui.askewly.com/llms.txt` 200 + 링크 표본 fetch 기록 (verify: curl 결과, Evidence로 기록)

## 결정 로그

1. **raw URL 설계** — llms.txt 링크가 가리키는 곳. **추천: 사이트 정적 서빙**(`ui.askewly.com/llms/<asset>` — build 시 SSOT 사본을 public/llms/로 복사, Geist식 same-source 파생, 레포 공개 여부와 무관, C-10 정합). 대안: GitHub raw URL(레포 public 필요, 배포와 버전 어긋날 수 있음). → 사용자 확정 대기.
2. **배포 승인** — push = Cloudflare Pages 자동 배포. 계획 승인과 별개로 Step 2 push 직전에 로컬 검증 결과 보고 후 승인 1회. → 절차 확정(추가 질문 아님).

이 외 예상 사용자 결정: 없음.

## planning_gate (요약)

- team_validation_mode: manual-pass
- delegation_decision: remote_background_agents=skip — 단일 스크립트+정적 자산 작업, 검증 커맨드 명확, fan-out 불필요. target_roles=[]
- spec_delta: 없음 — horizon doc·ROADMAP AG1에 이미 계약 기록 (spec_skip_reason: 이번 run은 그 계약의 이행)
- dod: 무결성 검사 PASS / 로컬 build+fetch 200 / 실배포 curl 200 / **실패 모드**: 존재하지 않는 SSOT 경로를 인덱스가 가리키면 생성 스크립트가 비정상 종료(exit≠0)함을 확인
