# 완료 — M26 CLI 0.4.0 출고 — 킥스타트 라이브 소비 마개

> work-id: m26-cli-release-040 · plan: archive/plans/2026-08-04-m26-cli-release-040.md · changeset: changesets/20260804-m26-cli-release-040 · 2026-08-04

## 1. 결과

- `@askewly/design` **0.4.0 npm 라이브** (minor — init --block 킥스타트·--color cosmos·번들 데이터 terms 563/recipes 48). Trusted Publishing(OIDC) 경로, 토큰 발급·폐기 없음.
- **킥스타트 라이브 소비 마개 해소**: 레포 밖에서 `npx @askewly/design@0.4.0 init … --block saas-app-shell --color cosmos` 가 `--registry` 플래그 없이 라이브 `ui.askewly.com/r` 를 소비해 전 구간(브리프→DESIGN.md→이식 34파일→requiredCssVars 28/28→verify PASS) 완주 — M19 의 "로컬 한정" 제약이 풀렸다.
- cosmos 팔레트 왕복 실증: 산출 DESIGN.md 에 `primary: "#3E5F94"` — M22 채굴(brain)→M23 프리셋→npm 출고→신선 환경 착지.
- 절차 정본(cli-release-procedure.md)에 0.4.0 실적 추기.
- 구현됨·미검증: 없음 — DoD 전 항목 실측.

## 2. 이슈와 해결

- 이슈 없음 — 워크플로 1회 통과(run 30902648076, conclusion success). 전파 지연도 없었다(즉시 200).
- finding 승계: CLI verify chart.tsx 속성 셀렉터 오탐 — 0.4.x patch 후보로 유지.
- 크기 회고: milestone 판정 적정 경계 — step 2(게이트+버전 / 출고+실증)로 최소 구성이나, 공개 레지스트리 비가역 출고 + 레포 밖 통합 실증이라 단독 capability 성립. changeset 1 디렉터리·커밋 2.

## 3. 증거

- 실표면: npm 공개 레지스트리 — `npm view @askewly/design version` = 0.4.0, `https://registry.npmjs.org/@askewly%2fdesign` HTTP 200 + 레포 밖 실디렉터리에서 npx 실행.
- 재현: `gh workflow run publish-cli.yml --ref main && gh run watch <id> --exit-status` → `npx --yes @askewly/design@0.4.0 verify <dir>` / `npx --yes @askewly/design@0.4.0 init <dir> --block saas-app-shell --color cosmos --yes`.
- 배포 전 게이트: build(data 번들) · vitest 60/60 · tsc --noEmit exit 0 · pack dry-run 15 files·258.2kB(dist/·data/ 12, src/·test/·scripts/ 부재 — +1파일 = M19 kickstart 모듈).
- 레포 밖 실증: verify 위반 dir exit 1(hex+slate 적발) / clean dir exit 0("verify PASS — 1 file(s) scanned") / 킥스타트 출력 "transplanted 34 file(s) from https://ui.askewly.com/r/saas-app-shell.json" · "required CSS variables: 28/28 defined" · "verify PASS — 11 block file(s), no color literals".
- 커밋: 54a78ac(step-1 버전·게이트) + 본 마감 커밋 · 상세 evidence: evidence/harvest/m26-cli-040.md
