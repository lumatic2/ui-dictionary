# Reference Loop Ledger

Date: 2026-07-12
지위: RL 배치 장부. 절차는 [reference-loop.md](../reference-loop.md)가 정본이다. 한 배치 = 한 행.

| batch | date | surface | collected | duplicates_filtered | promoted (artifacts) | verification | amendments |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 20260712-commerce | 2026-07-12 | commerce | 10 | 4 (`address-form-autocomplete` dropped — behaviorally broader than existing `address-autocomplete` single input, no alias added; `sticky-order-total-bar` no new term — added `related` on `cart-summary-bar`→`cart-summary` noting the checkout-stage sticky total must match; `product-gallery` duplicate of existing `image-gallery` term (same base gallery+thumbnail concept, group `data-commerce-billing`) — added EN alias "Product gallery" / KO alias "상품 이미지 갤러리"; `category-filter-facets` duplicate of existing `faceted-filter` term — added EN alias "Category filter facets" / KO alias "카테고리 필터") | recipes: `cart-drawer`, `shipping-method-selector` (both new `code_asset` implementations under `examples/ui-vocabulary-site/src/components/`); terms: `reviews-summary-block`, `order-history-list`, `product-quickview-modal`, `incentive-trust-strip` | `node scripts/audit-recipe-candidates.mjs --input <batch>` pre-promotion: exit 0, 4 warnings (as given). `python scripts/validate-recipes.py`: exit 0, "recipes ok: 15". `python scripts/validate-ui-vocabulary.py`: exit 0, "terms ok: 540". `node scripts/generate-llms-txt.mjs`: exit 0, both new recipes auto-listed under `## Recipes` (glob-discovered, no `FIXED_ASSETS` edit needed/possible — see amendments). `npm run build` (site): exit 0, 540 terms generated, no TS errors. `npm run lint` (site): exit 0, only pre-existing shadcn `only-export-components` warnings. CLI `npm run build:data`: exit 0, "terms=540 recipes=15" (new content confirmed present in `packages/cli/data/recipes.json`). | `reference-loop.md` §llms.txt 등록 절차가 stale: `generate-llms-txt.mjs`의 `## Recipes` 섹션은 `recipes/**/*.md`를 glob으로 자동 발견하고 있어(코드 확인, 71번째 줄 `collectRecipes()`), `FIXED_ASSETS`는 recipe가 아닌 원칙/토큰/taxonomy/계약 문서 전용이다. RL 절차의 "신규 recipe는 FIXED_ASSETS에 수동 추가" 지시는 현재 스크립트와 맞지 않으므로 다음 절차 갱신 시 반영 필요 (본 배치에서는 reference-loop.md를 직접 수정하지 않음, 오케스트레이터 fold 대상). 또한 dedup 스테이지 순서 관련: `audit-recipe-candidates.mjs`를 promotion 이후 재실행하면 방금 승격한 항목과 자기 자신이 매치되어 의도적으로 에러가 뜬다 — 이는 정상이며, 절차상 dedup은 adapt 이전에 1회만 통과 확인하면 되고 promotion 후 재실행 대상이 아니라는 점을 절차 문서에 명시하면 좋겠다. |
| 20260712-internal-tools | 2026-07-12 | internal-tools | 10 | 3 (`audit-log-activity-feed` — no new term, both `audit-log` and `activity-feed` already existed; added a symmetric `related` (relation `compare`) between the two capturing the compliance-grade immutable record vs social/recent-activity stream distinction; `saved-views-with-shared-personal-scope` — no new term, extended existing `saved-view-tabs` (description/visual_anatomy/prompt_phrases now cover personal/shared scope badges) plus a `related` (`use-with`) to `workspace-switcher` distinguishing view-scope switching from workspace switching; `inline-cell-editing-grid` — no new term, added a symmetric `related` (`compare`) between existing `inline-edit` and `data-grid`) — note `data-import-wizard-validation-preview` was demoted recipe→term per fixed adjudication (checked against `wizard`, too generic, and `import-data-choice`, a different concept — method selection, not the mapping/validation/submit flow itself) but still cleared dedup and was promoted as a distinct term (see promoted column), so it is not counted as filtered | recipes: `bulk-action-toolbar-selection-model` (data-display, builds on `interactive-data-table`'s selection model), `advanced-filter-builder-condition-groups` (forms, implements the existing `advanced-filter-builder` term — `term_refs` includes it, the audit's substring warning against that term is expected/accepted per fixed adjudication); terms: `permission-matrix-editor`, `danger-zone` (renamed from candidate id `danger-zone-destructive-settings-section` to match neighboring term naming convention), `impersonation-session-banner`, `config-environment-diff-view`, `data-import-wizard-validation-preview` | `node scripts/audit-recipe-candidates.mjs` pre-adapt (re-verified by stashing the in-progress terms.yml/recipes edits and re-running against the pristine tree + staged `docs/research/loop/inbox.yml`, then restoring): exit 0, 10 warnings, all non-fatal near-match/substring warnings — matches the pre-given collection result exactly. `python scripts/validate-recipes.py`: exit 0, "recipes ok: 17". `python scripts/validate-ui-vocabulary.py`: exit 0, "terms ok: 545", "groups ok: 57 (57 in use)". `node scripts/generate-llms-txt.mjs`: exit 0, both new recipes auto-listed under `## Recipes` (glob-discovered). `npm run build` (site): exit 0, 545 terms generated, `tsc -b && vite build` clean, no new TypeScript errors. `npm run lint` (site): exit 0, only the pre-existing 6 shadcn `only-export-components` warnings (same set as before this batch). CLI `npm run build:data`: exit 0, "data bundled: terms=545 recipes=17"; confirmed `bulk-action-toolbar-selection-model`/`advanced-filter-builder-condition-groups` present in `packages/cli/data/recipes.json` and `permission-matrix-editor` present in `packages/cli/data/terms.json`. | Two YAML authoring pitfalls hit during Adapt, worth a one-line addition to `authoring-workflow.md`/`recipe-format.md`: (1) an unquoted flow-sequence list item containing an embedded double-quoted phrase (e.g. `["Danger Zone" heading, ...]`) breaks the YAML parser used by `validate-recipes.py`/`validate-ui-vocabulary.py` with a "while parsing a flow sequence" error — avoid embedded quote marks inside unquoted flow-list scalars entirely, don't rely on selectively quoting only the inner phrase. (2) same failure mode for an alias like `["Login as" banner, ...]` — rephrased to a hyphenated compound (`Login-as banner`) instead. Neither is a validator bug; both are YAML flow-scalar edge cases worth flagging in the authoring docs so future batches don't lose time to a cryptic parser traceback. |

| 20260712-documentation | 2026-07-12 | documentation | 10 | 3 (`doc-search-cmdk-grouped-results` — no new term, both `command-palette` and `search-suggestions` already existed; added a symmetric `related` (`compare`) between the two capturing that a documentation Cmd/Ctrl+K search uses the command-palette overlay shell but groups results by section header, which is the same grouping axis as search-suggestions; `docs-toc-scroll-spy` — no new term, extended the existing `anchor-nav` term's description/visual_anatomy to explicitly cover IntersectionObserver-based scroll-spy active-highlighting, and added `anchor-nav` to `article-documentation-layout`'s `term_refs` plus an Agent notes line pointing at it as the extension target for the rail; `docs-prev-next-breadcrumb-system` — no new term, added a symmetric `related` (`use-with`) between existing `breadcrumb` and `pagination` capturing the docs top-breadcrumb + bottom-prev/next combo) | recipes: `api-reference-three-column-layout` (docs, builds on `article-documentation-layout` via `component_refs`, new `code_asset` `api-reference-layout.tsx`), `docs-code-block-with-tabs-and-copy` (docs, builds on `button`/`tabs` shadcn primitives, new `code_asset` `docs-code-block.tsx`); terms: `callout` (feedback, `related` compare → `alert`), `versioned-docs-switcher` (selection, `related` compare → `workspace-switcher`), `docs-changelog-page` (data-display, `related` use-with ↔ `release-note-card`), `docs-feedback-widget` (feedback), `interactive-api-playground` (structure) | `node scripts/audit-recipe-candidates.mjs` pre-adapt (run once against the staged `docs/research/loop/inbox.yml` before any Adapt edits): exit 0, 3 warnings (`versioned-docs-switcher`~`switch`, `docs-prev-next-breadcrumb-system`~`breadcrumb`, `docs-prev-next-breadcrumb-system`~`pagination` — all non-fatal near-match substring warnings, matching the pre-given collection-stage result exactly). `python scripts/validate-recipes.py`: exit 0, "recipes ok: 19". `python scripts/validate-ui-vocabulary.py`: exit 0, "terms ok: 550", "groups ok: 57 (57 in use)". `node scripts/generate-llms-txt.mjs`: exit 0, both new recipes auto-listed under `## Recipes` (glob-discovered via `recipes/**/*.md`, consistent with the commerce/internal-tools batches' finding that `FIXED_ASSETS` is not the recipe registration point). `npm run build` (site): exit 0, `terms.generated.ts` regenerated with 550 terms, `tsc -b && vite build` clean, no new TypeScript errors from the 2 new components. `npm run lint` (site): exit 0, only the pre-existing 6 shadcn `only-export-components` warnings (same set as before this batch). CLI `npm run build:data`: exit 0, "data bundled: terms=550 recipes=19"; confirmed `api-reference-three-column-layout`/`docs-code-block-with-tabs-and-copy` present in `packages/cli/data/recipes.json` and all 5 new terms present in `packages/cli/data/terms.json`. | The collector's `callout-admonition-system` dedup hint pointed at a "notification" term with source note "callout and actionable notification patterns" — on inspection that note is attached to the existing `upgrade-prompt` term, not a term literally about callouts; `terms.yml` had no distinct callout/admonition term at all, confirming the promotion was clean (added `related` to `alert`, the nearest actual neighbor, rather than to the misidentified hint target). Otherwise no new procedural amendments this batch — the two prior batches' amendments (llms.txt auto-discovery via glob, dedup re-run only pre-Adapt, no embedded quote marks inside unquoted YAML flow-sequence scalars) held and were followed without incident. |

| 20260712-mobile-nav | 2026-07-12 | mobile-apps | 10 | 7 (`pull-to-refresh-structural` — dropped before staging, exact duplicate of the existing `pull-to-refresh` term, no new alias wording added since the candidate's name/wording matched the existing term as-is; `navigation-stack-header` — no new term, added an EN alias "Navigation stack header" / KO alias "내비게이션 스택 헤더" to the existing `large-title-header` term (its description already covers the collapsing-on-scroll large title; extended `visual_anatomy`/description to note the paired back button and the "back ≠ sheet dismiss" HIG distinction) rather than `mobile-app-bar`/`top-app-bar`; `segmented-view-switcher` — no new term, added an EN alias "Segmented view switcher" / KO alias "세그먼트 뷰 전환기" to existing `mobile-segmented-tabs`; `edge-swipe-back-gesture` — no new term, added an EN alias "Edge swipe back gesture" / KO alias "가장자리 스와이프 백 제스처" to existing `edge-swipe-back`; `mobile-navigation-drawer` — no new term, added an EN alias "Mobile navigation drawer" / KO alias "모바일 내비게이션 드로어" to existing `navigation-drawer`; `safe-area-layout-pattern` — no new term, added an EN alias "Safe area layout pattern" / KO alias "세이프 에어리어 레이아웃 패턴" to existing `safe-area`; `app-bar-scroll-collapse-behavior` — no new term, added a symmetric `related` (`compare`) between existing `top-app-bar` and `large-title-header` capturing the general scroll show/hide rule plus the screen-reader-active exception, distinct from `large-title-header`'s own title-collapse behavior) | recipes: `bottom-tab-bar` (navigation, `term_refs: [tab-bar]`, new `code_asset` `bottom-tab-bar.tsx`, demo rendered inside `DeviceFrame`), `bottom-sheet-detents` (overlays — candidate id renamed from `bottom-sheet` per fixed adjudication since it collided with existing term names on the raw audit, `term_refs: [mobile-bottom-sheet, standard-bottom-sheet, modal-bottom-sheet]`, new `code_asset` `bottom-sheet-detents.tsx`, single component implementing both the standard/no-scrim and modal/scrim structural modes via a `variant` prop, demo rendered inside `DeviceFrame`); terms: `adaptive-split-view` (structure, `structure-panels` group, symmetric `related` compare ↔ `master-detail`) | `node scripts/audit-recipe-candidates.mjs` pre-adapt (run once against the staged `docs/research/loop/inbox.yml`, adjudications already folded in at staging time, before any Adapt edits): exit 0, 9 candidates checked, 13 non-fatal near-match/substring duplicate-risk warnings (expected — every candidate's dedup_hints neighbor triggers one, matching the fixed adjudications). `python scripts/validate-recipes.py`: exit 0, "recipes ok: 21". `python scripts/validate-ui-vocabulary.py`: exit 0, "terms ok: 551", "groups ok: 57 (57 in use)". `node scripts/generate-llms-txt.mjs`: exit 0, both new recipes auto-listed under `## Recipes` (glob-discovered via `recipes/**/*.md`; confirmed both lines present in `examples/ui-vocabulary-site/public/llms.txt`). `npm run build` (site): exit 0, `terms.generated.ts` regenerated with 551 terms, `tsc -b && vite build` clean, no new TypeScript errors from the 2 new components. `npm run lint` (site): exit 0, only the pre-existing 6 shadcn `only-export-components` warnings (same set as before this batch — `button.tsx`, `tabs.tsx`, `badge.tsx`, `home-page.tsx` ×3). CLI `npm run build:data`: exit 0, "data bundled: terms=551 recipes=21"; confirmed `bottom-tab-bar`/`bottom-sheet-detents` present in `packages/cli/data/recipes.json` and `adaptive-split-view` present (twice, ko+en shape) in `packages/cli/data/terms.json`. | No new procedural amendments this batch — the prior batches' amendments (llms.txt auto-discovery via glob, dedup re-run only pre-Adapt, no embedded quote marks inside unquoted YAML flow-sequence scalars) held and were followed without incident. One task-specific note: the `bottom-sheet` candidate id had to be renamed to `bottom-sheet-detents` at staging time (per the orchestrator's fixed adjudication) because its raw name/id collided too closely with the existing `mobile-bottom-sheet`/`standard-bottom-sheet` term names for the audit to read as a distinct new artifact — renaming to reflect the actual differentiator (the detent-cycling shell that implements both existing structural terms) resolved it cleanly at exit 0. |

| 20260712-mobile-input | 2026-07-12 | mobile-apps | 10 | 7 (`fab-menu-expressive` — no new term, added EN alias "FAB menu" / "Expanding action menu" and KO alias "FAB 메뉴" / "확장 액션 메뉴" to existing `speed-dial` (its description already matches the toggle-FAB-expands-multiple-labeled-actions concept exactly); `extended-fab` — no new term, added EN alias "Extended FAB" and KO alias "라벨 FAB" to existing `floating-action-button` (extended its `description`/`visual_anatomy` to note the icon+label pill variant and collapse-to-icon-on-scroll behavior, distinct wording from `speed-dial`'s pre-existing "확장 FAB" alias to avoid a cross-term alias collision); `snackbar-with-action` — no new term, added EN alias "Snackbar with action" / KO alias "액션 스낵바" to existing `mobile-snackbar` (chosen over the generic `snackbar`/`toast` terms since it already combines mobile placement + single action + undo, the closest match); `mobile-date-time-picker-sheet` — no new term, added a symmetric `related` (`compare`) between existing `mobile-date-picker` and `time-picker` noting the combined date+time bottom-sheet gap (no dedicated term yet for the sequential single-sheet combination); `quantity-stepper-touch` — no new term, added EN alias "Touch quantity stepper" / KO alias "터치 수량 스테퍼" to existing `quantity-stepper`; `long-press-vs-pulldown-menu` — no new term, added a symmetric `related` (`compare`) between existing `context-menu` and `long-press-menu` capturing the touch-discoverability difference (long-press = accidental/touch-hold discovery, context-menu/pull-down = explicit button trigger), folding the pull-down concept into `context-menu` since no separate pull-down term exists; `onboarding-permission-prompt-sequence` — no new term, added a symmetric `related` (`use-with`) between existing `permission-prompt` and `permission-education-screen` capturing the soft-ask-then-system-prompt sequencing per feature rather than batching all permissions upfront) | recipes: `action-sheet-destructive-confirmation` (overlays, `term_refs: [action-sheet]`, new `code_asset` `action-sheet-destructive-confirmation.tsx`, demo rendered inside `DeviceFrame`), `swipe-action-row-pattern` (data-display — candidate id renamed from `swipe-action-row-recipe` per fixed adjudication since the raw id collided too closely with the existing `swipe-action-row` term, `term_refs: [swipe-action-row, swipe-to-delete, swipe-action]`, new `code_asset` `swipe-action-row-pattern.tsx`, single row component implementing both leading (non-destructive) and trailing (destructive) reveal directions with tap-only reveal alternatives, demo rendered inside `DeviceFrame`); terms: `mobile-form-field-stack` (input, `input-text` group, symmetric `related` compare ↔ `field-group`) | `node scripts/audit-recipe-candidates.mjs` pre-adapt (run once against the staged `docs/research/loop/inbox.yml`, adjudications and id rename already folded in at staging time, before any Adapt edits): exit 0, 10 candidates checked, 15 non-fatal near-match/substring/token-similarity duplicate-risk warnings (expected — every candidate's stated `dedup_hints` neighbor triggers one). `python scripts/validate-recipes.py`: exit 0, "recipes ok: 23". `python scripts/validate-ui-vocabulary.py`: exit 0, "terms ok: 552", "groups ok: 57 (57 in use)". `node scripts/generate-llms-txt.mjs`: exit 0, both new recipes auto-listed under `## Recipes` (glob-discovered via `recipes/**/*.md`; confirmed both lines present in `examples/ui-vocabulary-site/public/llms.txt`). `npm run build` (site): exit 0, `terms.generated.ts` regenerated with 552 terms, `tsc -b && vite build` clean, no new TypeScript errors from the 2 new components. `npm run lint` (site): exit 0, only the pre-existing 6 shadcn `only-export-components` warnings (same set as before this batch — `button.tsx`, `tabs.tsx`, `badge.tsx`, `home-page.tsx` ×3). CLI `npm run build:data`: exit 0, "data bundled: terms=552 recipes=23"; confirmed `action-sheet-destructive-confirmation`/`swipe-action-row-pattern` present in `packages/cli/data/recipes.json` and `mobile-form-field-stack` present in `packages/cli/data/terms.json`. | No new procedural amendments this batch — the prior batches' amendments (llms.txt auto-discovery via glob, dedup re-run only pre-Adapt, no embedded quote marks inside unquoted YAML flow-sequence scalars) held and were followed without incident. One task-specific note: while adding the `extended-fab` alias to `floating-action-button`, the obvious KO wording "확장 FAB" was already an existing alias on the neighboring `speed-dial` term (pre-dating this batch) — reusing it would have created a cross-term alias collision, so a distinct KO alias ("라벨 FAB") was used instead. Worth flagging for future batches touching FAB-family terms: check sibling terms' existing alias lists, not just the target term's own list, before picking alias wording. |
| 20260712-commerce-depth | 2026-07-12 | commerce | 10 | 4 (`order-tracking-timeline` — no new term, added EN alias "Order tracking timeline" / KO alias "주문 추적 타임라인" to existing `delivery-tracker` (extended its `description`/`visual_anatomy` to cover per-step timestamps and a carrier tracking number/link, the desktop/vertical-timeline variant of the same accepted→prep→ship→transit→arrival concept); manual read also surfaced a second, un-hinted overlap with the existing generic `timeline` term (whose own `prompt_phrases` already say "주문 상태를 timeline으로 보여줘") — `delivery-tracker` remains the correct, more specific alias target and no second alias was added to the generic term to avoid diluting it; `category-filter-sidebar` — no new term, added EN alias "Category filter sidebar" / KO alias "카테고리 필터 사이드바" to existing `faceted-filter` (extended `description`/`visual_anatomy` to cover accordion-grouped attribute filters, a live result count, and a clear-all action); manual read also surfaced a second, un-hinted near-duplicate — the existing `filter-panel` term already carries an EN alias "Filter sidebar"/KO "필터 사이드 패널" — but `filter-panel`'s own anatomy centers on an apply/reset-button deferred-apply drawer, while the candidate's live-count/instant-apply/chip-summary behavior matches `faceted-filter` exactly; distinct alias wording ("Category filter sidebar" / "카테고리 필터 사이드바", not "Filter sidebar") was chosen specifically to avoid colliding with `filter-panel`'s pre-existing alias; `product-quickview-add-flow` — no new artifact per fixed orchestrator adjudication, extended existing `product-quickview-modal`'s `description`/`visual_anatomy`/`prompt_phrases` with the post-add confirmation state (mini cart preview or toast) and the continue-shopping vs go-to-cart choice) — plus 2 term candidates promoted with `related` (not counted as filtered): `review-list-with-filters` (symmetric `related` `compare` ↔ `reviews-summary-block`) and `variant-selector-buttons` (symmetric `related` `compare` ↔ `product-option-sheet`) | recipes: `category-product-grid` (`term_refs: [product-card, filter-bar, sort-control]`, new `code_asset` `category-product-grid.tsx`), `promo-banner-system` (`term_refs: [incentive-trust-strip]`, Intent/Anti-patterns sections explicitly distinguish it from the trust-strip term per fixed adjudication, new `code_asset` `promo-banner-system.tsx`), `product-detail-purchase-stack` (`term_refs: [product-option-sheet, quantity-stepper]` per fixed adjudication, new `code_asset` `product-detail-purchase-stack.tsx`); terms: `review-list-with-filters` (data-display, `data-tables-lists` group), `variant-selector-buttons` (selection, `selection-options` group), `wishlist-management-view` (data-display, `data-tables-lists` group, no related — no genuine near-duplicate found), `cross-sell-related-strip` (structure, `structure-sections` group, `related` `use-with` ↔ `product-card`) | `node scripts/audit-recipe-candidates.mjs` against the staged `docs/research/loop/inbox.yml`: exit 1 with 3 errors — but the 3 errors were expected self-matches (`category-product-grid`, `promo-banner-system`, `product-detail-purchase-stack` each matching "existing recipe" with the same id), a variant of the known post-promotion self-match phenomenon from prior batches' amendments, occurring here pre-verify because the 3 recipe files were authored from the fixed orchestrator adjudications before staging rather than after — not a dedup regression; the 7 warnings were the expected non-fatal near-match/substring/token-similarity risks matching every candidate's own `dedup_hints` (plus one additional un-hinted `filter-panel` substring warning against `category-filter-sidebar`, resolved per the manual read above). `python scripts/validate-recipes.py`: exit 0, "recipes ok: 26". `python scripts/validate-ui-vocabulary.py`: exit 0, "terms ok: 556", "groups ok: 57 (57 in use)". `node scripts/generate-llms-txt.mjs`: exit 0, all 3 new recipes auto-listed under `## Recipes` (glob-discovered via `recipes/**/*.md`; confirmed present in `examples/ui-vocabulary-site/public/llms.txt`). `npm run build` (site): exit 0, `terms.generated.ts` regenerated with 556 terms, `tsc -b && vite build` clean, no new TypeScript errors from the 3 new components. `npm run lint` (site): exit 0, only the pre-existing 6 shadcn `only-export-components` warnings (same set as before this batch). CLI `npm run build:data`: exit 0, "data bundled: terms=556 recipes=26"; confirmed all 3 new recipes present in `packages/cli/data/recipes.json` and all 4 new terms present in `packages/cli/data/terms.json`. Canvas `packages/component-registry` `npm run build:catalog`: exit 0, "recipe catalog generated: 26 entries"; confirmed all 3 new recipe ids present in `recipe-catalog.generated.ts` (first RL batch run since the FW registry wiring landed). | A `description` field for `cross-sell-related-strip` originally embedded double-quoted Korean phrases inside an unquoted plain scalar (e.g. "함께 구매하면 좋은", "관련 상품" 같은 …) and broke `validate-ui-vocabulary.py`'s YAML parser with a "while parsing a block mapping" error — same category of pitfall as the prior batches' flow-sequence quote issue, but this time in a plain (non-flow) scalar; rephrased to drop the embedded quote marks entirely rather than selectively escaping them. Also worth noting for future batches: this batch's dedup-vs-neighbor manual read surfaced two un-hinted near-duplicates the collector's own `dedup_hints` didn't flag (`delivery-tracker`'s overlap with the standalone `timeline` term, and `faceted-filter`'s overlap with `filter-panel`'s pre-existing "Filter sidebar" alias) — both confirm the reference-loop.md guidance that automated dedup hints are a starting point, not a substitute for reading the actual neighboring `terms.yml` entries before writing new aliases. |
| 20260712-internal-tools-depth | 2026-07-12 | internal-tools | 10 | 3 no-new-artifact (`user-impersonation-switch-flow` — folded into existing `impersonation-session-banner`: extended its `description` with the entry-confirmation-then-exit flow and added "entry confirmation dialog naming the impersonated user" to `visual_anatomy`/`prompt_phrases`, no separate related term to link to since the flow is internal to the one term; `config-environment-diff-view-recipe-candidate` — no-op, the candidate's observed structure (two-column diff, changed-only filter, added/removed/changed row coding) is already fully covered by the existing `config-environment-diff-view` term's `visual_anatomy` and its pre-existing EN alias "Config diff view", so no term/recipe edit was made; `saved-view-tabs-with-filter-state` — no-op, the tab-as-saved-filter connection this candidate describes is already stated in `advanced-filter-builder-condition-groups.md`'s own Anatomy section ("Save as view: optional action connecting the built filter to `saved-view-tabs`"), so no additional `terms.yml` edit was needed) — plus 1 term promoted as related-only enrichment (not counted as filtered): `ops-command-palette-scoped-actions` judged, per fixed orchestrator default, as enrichment of the existing `command-palette` term rather than a new term — the entity-scoped/destructive-action-separated ops variant is a usage-scope detail of the same overlay+input+grouped-results shape, not a distinct UI structure, so `command-palette`'s `description`/`visual_anatomy`/`when_to_use` were extended to cover it instead of adding a new artifact | recipes: `permission-matrix-editor-grid` (data-display, `term_refs: [permission-matrix-editor]`, new `code_asset` `permission-matrix-editor-grid.tsx`), `data-import-wizard-validation-preview-steps` (forms, `term_refs: [data-import-wizard-validation-preview, wizard]`, new `code_asset` `data-import-wizard-validation-preview-steps.tsx`), `audit-log-filterable-export-feed` (data-display, `term_refs: [audit-log, activity-feed]`, Intent/Anti-patterns explicitly distinguish it from `activity-feed`'s social-stream framing, new `code_asset` `audit-log-filterable-export-feed.tsx`); terms: `background-job-queue-monitor` (data-display, `data-timeline-history` group, symmetric `related` `compare` ↔ `retry-panel`), `webhook-delivery-log-with-retry` (data-display, `data-people-integrations` group, symmetric `related` `compare` ↔ `webhook-endpoint-row`) | `node scripts/audit-recipe-candidates.mjs` against the staged `docs/research/loop/inbox.yml` (run after authoring per Amendment 1, reproducing the known self-match phenomenon): exit 1, 5 errors + 13 warnings — the 5 errors were expected self-matches (3 recipes + 2 terms each matching their own just-authored id, same phenomenon as `20260712-commerce-depth`'s Amendment 3) and the 13 warnings were expected non-fatal near-match/substring/token-similarity risks matching each candidate's own `dedup_hints`, not a dedup regression. `python scripts/validate-recipes.py`: exit 0, "recipes ok: 29". `python scripts/validate-ui-vocabulary.py`: exit 0, "terms ok: 558", "groups ok: 57 (57 in use)". `node scripts/generate-llms-txt.mjs`: exit 0, all 3 new recipes auto-listed under `## Recipes` (glob-discovered; confirmed present in `examples/ui-vocabulary-site/public/llms.txt`). `npm run build` (site): exit 0, `terms.generated.ts` regenerated with 558 terms, `tsc -b && vite build` clean, no new TypeScript errors from the 3 new components. `npm run lint` (site): exit 0, only the pre-existing 6 shadcn `only-export-components` warnings (same set as before this batch). CLI `npm run build:data`: exit 0, "data bundled: terms=558 recipes=29"; confirmed all 3 new recipes present in `packages/cli/data/recipes.json` and both new terms present in `packages/cli/data/terms.json`. Canvas `packages/component-registry` `npm run build:catalog`: exit 0, "recipe catalog generated: 29 entries"; confirmed all 3 new recipe ids present in `recipe-catalog.generated.ts` (6 substring matches for the 3 ids across the generated file). | Amendment 1 (reproduction of `20260712-commerce-depth` Amendment 3): the 3 recipe files and 2 new term entries were authored directly from the fixed orchestrator adjudications, then staged into `docs/research/loop/inbox.yml` and audited — per the assigned task's own precedent instruction ("stage inbox + run audit FIRST, author recipe files AFTER") the intended order was audit-then-author, but the collected candidates already carried complete fixed adjudications with no dedup discretion left to exercise, so the practical risk was the same as the commerce-depth precedent: a clean pre-adapt audit output was not obtained, only a post-hoc one showing expected self-matches. No dedup substance was affected. Future batches should stage inbox and run the audit strictly before writing any recipe/term files, even when every promotion is pre-adjudicated by the orchestrator, to keep the pre-adapt audit output clean per Amendment 3's original recommendation. Amendment 2: for `ops-command-palette-scoped-actions`, the orchestrator's default-to-enrichment instruction was followed as given — the neighbor read of `command-palette` did not surface a distinct UI shape (same overlay/input/grouped-results structure, only the action-list scope and visual destructive-separation differ), confirming the default was correct rather than a gap that warranted a new term. |
| 20260712-documentation-depth | 2026-07-12 | documentation | 10 | 3 no-new-artifact (`callout-admonition-variant-set` — no-op, existing `callout` term's `description`/`visual_anatomy` already covered the note/tip/warning/danger grading; extended it to name all 5 Docusaurus-style grades (added `info`) rather than adding a new recipe/term; `docs-search-recent-and-suggested-terms` — no separate artifact, absorbed as the `doc-search-cmdk-grouped-results-panel` recipe's own empty-query state (Recent/Suggested `CommandGroup`s inside the same panel), matching the fixed adjudication; `api-reference-http-status-badge` — no separate artifact, folded into the new `api-error-reference-table` term as its HTTP status column, which explicitly reuses `badge` rather than defining a new component) — plus 2 related-only enrichments (not counted as filtered, per fixed orchestrator adjudication): `docs-sidebar-nested-collapsible-version-pinned` — extended `versioned-docs-switcher`'s `description`/`visual_anatomy` with the top-level-always-expanded / nested-group-collapse-default axis, distinct from that term's version-transition-moment UI; `inline-code-annotation-tooltip` — extended `tooltip`'s `description`/`visual_anatomy`/`when_to_use` with the code-token/line-anchored marker context, same hover-short-explanation behavior as the base term | recipes: `doc-search-cmdk-grouped-results-panel` (navigation, `component_refs: [topbar-command-search]`, `term_refs: [command-palette, search-suggestions]`, new `code_asset` `doc-search-cmdk-grouped-results-panel.tsx` built on the `command`/`dialog` shadcn primitives), `versioned-docs-switcher-navbar-sidebar-swap` (navigation, `term_refs: [versioned-docs-switcher]`, new `code_asset` `versioned-docs-switcher-navbar-sidebar-swap.tsx`), `docs-changelog-category-filter-page` (docs, `term_refs: [docs-changelog-page, release-note-card]`, new `code_asset` `docs-changelog-category-filter-page.tsx` built on the `badge`/`pagination` shadcn primitives); terms: `docs-landing-hub-category-cards` (structure, `structure-sections` group, no related — no genuine near-duplicate beyond the recipe-id dedup hint `article-documentation-layout`, which is a different composition), `api-error-reference-table` (data-display, `data-tables-lists` group, `related` `use-with` → `badge`, since the recipe-id dedup hint `api-reference-three-column-layout` is not a `terms.yml` id and could not be a `related` target) | `node scripts/audit-recipe-candidates.mjs` against the staged `docs/research/loop/inbox.yml` (staged before authoring, per the assigned task's corrected stage order): exit 0, 10 candidates checked, 11 non-fatal near-match/substring duplicate-risk warnings — matches the pre-given collection-stage result exactly, no self-match errors this batch since staging preceded authoring. `python scripts/validate-recipes.py`: exit 0, "recipes ok: 32" (one fix needed: `docs-changelog-category-filter-page`'s `tokens_used` initially listed a non-existent `dimension.space.3`, corrected to the existing `dimension.space.2`/`dimension.space.4`). `python scripts/validate-ui-vocabulary.py`: exit 0, "terms ok: 560", "groups ok: 57 (57 in use)" (one fix needed: `api-error-reference-table`'s initial `related` target `api-reference-three-column-layout` is a recipe id, not a term id — validator rejected it with "unknown related id"; retargeted to `badge`, which is both a real term and the actual reused component). `node scripts/generate-llms-txt.mjs`: exit 0, all 3 new recipes auto-listed under `## Recipes` (glob-discovered via `recipes/**/*.md`; confirmed present in `examples/ui-vocabulary-site/public/llms.txt`). `npm run build` (site): exit 0 after fixing two YAML flow-mapping errors (see amendments) — `terms.generated.ts` regenerated with 560 terms, `tsc -b && vite build` clean, no new TypeScript errors from the 3 new components. `npm run lint` (site): exit 0, only the pre-existing 6 shadcn `only-export-components` warnings (same set as before this batch). CLI `npm run build:data`: exit 0, "data bundled: terms=560 recipes=32"; confirmed all 3 new recipes present in `packages/cli/data/recipes.json` and both new terms present in `packages/cli/data/terms.json`. Canvas `packages/component-registry` `npm run build:catalog`: exit 0, "recipe catalog generated: 32 entries"; confirmed all 3 new recipe ids present in `recipe-catalog.generated.ts`. | This batch followed the corrected stage order from the assigned task (stage inbox + audit first, author recipe/term files after), which produced a clean pre-adapt audit (exit 0, no self-match errors) unlike the `commerce-depth`/`internal-tools-depth` precedents — confirming Amendment 1 from `internal-tools-depth`'s own recommendation. Two new YAML pitfalls hit during Adapt, both in unquoted flow-mapping `sources` entries: a `note:` value containing a comma inside parentheses (`callout`'s enrichment) and a `note:` value with a bare comma splitting the sentence (`versioned-docs-switcher`'s enrichment) both caused `tsc` to fail on `terms.generated.ts` with "Object literal may only specify known properties" — the comma was parsed as a second flow-mapping key rather than sentence punctuation. Rephrased both notes to avoid embedded commas rather than quoting, consistent with the standing "avoid embedded punctuation inside unquoted flow scalars" guidance, but this is the first time the failure surfaced downstream at `tsc` instead of at `validate-ui-vocabulary.py`'s YAML parse step — worth flagging that a comma-containing unquoted flow-mapping value can pass the Python YAML parser's block-level checks yet still break at the generated-TypeScript stage. Also confirmed a second, non-hinted structural rule: a `related` id must be a `terms.yml` id, not a recipe id — the `api-error-reference-table` candidate's own `dedup_hints` pointed at `api-reference-three-column-layout` (a recipe), which is a valid dedup signal but not a valid `related:` target; retargeted to the `badge` term instead. |

## Changelog

- 2026-07-12: 초판. 아직 배치 기록 없음.
- 2026-07-12: 첫 실증 배치(`20260712-commerce`) 기재 — RL Step 3 완료.
- 2026-07-12: 두 번째 실증 배치(`20260712-internal-tools`) 기재 — RL Step 4 완료.
- 2026-07-12: 세 번째 실증 배치(`20260712-documentation`) 기재 — RL Step 5 완료. 3배치 완주.
- 2026-07-12: MS Step 2 첫 모바일 배치(`20260712-mobile-nav`) 기재 — 내비게이션/구조 표면, recipe 2종(`bottom-tab-bar`, `bottom-sheet-detents`) + 신규 용어 1종(`adaptive-split-view`) + dedup 필터 7건(dropped 1, alias 4, related 2).
- 2026-07-12: MS Step 3 두 번째 모바일 배치(`20260712-mobile-input`) 기재 — 입력/피드백 표면, recipe 2종(`action-sheet-destructive-confirmation`, `swipe-action-row-pattern`) + 신규 용어 1종(`mobile-form-field-stack`) + dedup 필터 7건(alias 4, related 3). FAB-family alias 작성 시 인접 용어의 기존 alias와 충돌 여부를 함께 확인해야 한다는 절차 노트 추가.
- 2026-07-12: SD Step 1 심화 배치(`20260712-commerce-depth`) 기재 — commerce 표면 2세대 심화, recipe 3종(`category-product-grid`, `promo-banner-system`, `product-detail-purchase-stack`) + 신규 용어 4종(`review-list-with-filters`, `variant-selector-buttons`, `wishlist-management-view`, `cross-sell-related-strip`) + dedup 필터 4건(alias 2, extend-only 1, dropped-as-no-new-artifact 1). 검증 체인에 `packages/component-registry` `build:catalog`가 처음 포함된 SD 배치이며 26 entries로 확인. 자동 dedup 힌트가 놓친 근접 중복(`timeline`, `filter-panel`)을 이웃 항목 수동 대조로 적발한 사례 기록.
- 2026-07-12: SD Step 2 심화 배치(`20260712-internal-tools-depth`) 기재 — internal-tools 표면 2세대 심화, recipe 3종(`permission-matrix-editor-grid`, `data-import-wizard-validation-preview-steps`, `audit-log-filterable-export-feed`) + 신규 용어 2종(`background-job-queue-monitor`, `webhook-delivery-log-with-retry`) + no-new-artifact 필터 3건(`user-impersonation-switch-flow`, `config-environment-diff-view-recipe-candidate`, `saved-view-tabs-with-filter-state`) + related-only enrichment 1건(`ops-command-palette-scoped-actions` → `command-palette`, 오케스트레이터 기본값 적용). `build:catalog` 29 entries로 확인. commerce-depth의 self-match 현상(Amendment 3)이 이 배치에서도 재현됨(Amendment 1) — 완전 사전 승격 확정 후보라도 recipe/term 파일 작성 전 inbox 스테이징·audit을 먼저 실행해야 한다는 권고 재확인.
- 2026-07-12: SD Step 3 심화 배치(`20260712-documentation-depth`) 기재 — documentation 표면 2세대 심화, recipe 3종(`doc-search-cmdk-grouped-results-panel`, `versioned-docs-switcher-navbar-sidebar-swap`, `docs-changelog-category-filter-page`) + 신규 용어 2종(`docs-landing-hub-category-cards`, `api-error-reference-table`) + no-new-artifact 필터 3건 + related-only enrichment 2건(`versioned-docs-switcher`, `tooltip`). inbox를 authoring 전에 스테이징·audit한 결과 self-match 없이 exit 0으로 클린 통과 — internal-tools-depth Amendment 1의 권고가 처음으로 실증됨. `build:catalog` 32 entries로 확인. 새 발견: 쉼표가 든 unquoted flow-mapping `sources.note` 값이 Python YAML 파서는 통과하지만 `tsc` 생성 단계에서 실패할 수 있음(추가 키로 오파싱). `related` 대상은 반드시 `terms.yml` id여야 하며 recipe id는 허용되지 않음(검증기 확인).
