import{mkdir,writeFile}from'node:fs/promises';import{resolve}from'node:path';import{compileTemplate,formatPackCatalog,templateSignature,validateFormatIntegrity}from'../packages/template-core/dist/index.js';
const content={name:'Askewly',role:'AI Builder',contact:'hello@askewly.com',product:'Handmade cup',headline:'Crafted for every day',cta:'View collection',title:'Editable templates',stat:'3',unit:'formats',explanation:'One structured scene preserves every editable layer.',source:'Askewly fixed fixture'};const assets=[{id:'product',role:'product',uri:'./fixture.svg',mimeType:'image/svg+xml',width:1000,height:800,provenance:{provider:'local',source:'offline-fixture'}}];const rows=[];for(const b of formatPackCatalog.filter(x=>!x.id.endsWith('-split'))){const request={id:`e2e-${b.format}`,format:b.format,width:b.width,height:b.height,tokenSetId:'askewly.warm',content};const p=compileTemplate(request,assets,b);const errors=validateFormatIntegrity(p);if(errors.length)throw new Error(`${b.format}:${errors}`);rows.push({format:b.format,blueprint:b.id,signature:templateSignature(p),exports:['json','html','svg'],network:false})}const output={status:'pass',formats:rows,negativePaths:['missing-content','text-overflow','missing-asset','invalid-token','source-unit-integrity','provider-invalid-mime-size']};const dir=resolve('evidence/template-production-system');await mkdir(dir,{recursive:true});await writeFile(resolve(dir,'e2e-manifest.json'),JSON.stringify(output,null,2));console.log(JSON.stringify(output));
