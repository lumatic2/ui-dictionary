# Step 0: Canonical Document Contract

Implemented in `packages/canvas-core/src/types.ts`, `validation.ts`, and `fixtures.ts`. Acceptance evidence: 1k/5k happy paths plus schema, hierarchy, source, and instance failure tests in `changesets/20260710-auc1-document-contract/README.md`.
