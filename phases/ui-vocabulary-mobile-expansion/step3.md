# Step 3: mobile-gestures-and-touch-affordances

## 작업

Batch 3 후보를 추가한다. 제스처는 보이지 않는 동작 자체보다 화면에 남는 affordance와 revealed state를 중심으로 표현한다.

## Acceptance Criteria

```bash
cd examples/ui-vocabulary-site
npm run build
npm run lint
```

- swipe/pull/long-press/drag 관련 용어가 한국어 자연어 query로 검색된다.
- 각 visual은 default/revealed 또는 before/after 상태를 카드 안에서 식별 가능하게 보여준다.

