# Step 2: mobile-overlays-and-system-feedback

## 작업

Batch 2 후보를 추가한다. bottom sheet 계열은 기존 `mobile-bottom-sheet`와 중복/related 관계를 먼저 정리한 뒤 세분화한다.

## Acceptance Criteria

```bash
cd examples/ui-vocabulary-site
npm run build
npm run lint
```

- `아래에서 올라오는 창`, `권한 요청 화면`, `공유 시트` 같은 query가 의도한 용어를 찾는다.
- sheet/dialog/prompt visual이 screen, scrim, layer 관계를 보여준다.

