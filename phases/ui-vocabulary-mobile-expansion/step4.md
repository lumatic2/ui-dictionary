# Step 4: mobile-inputs-pickers-and-auth

## 작업

Batch 4 후보를 추가한다. 입력/인증 패턴은 작동 가능한 작은 상태 변화가 있으면 interactive visual로 만든다.

## Acceptance Criteria

```bash
cd examples/ui-vocabulary-site
npm run build
npm run lint
```

- `인증번호 입력`, `숫자 비밀번호`, `날짜 휠`, `주소 검색` query가 관련 용어를 찾는다.
- OTP/passcode/picker visual이 실제 모바일 입력 형태를 보여준다.

